# PRAFTA AI 용어 설명 (쉬운 버전)

> `PRAFTA_MLOps_LLMOps_경험정리.md` 에 나오는 용어들을 PRAFTA 실제 예시와 함께 풀어쓴 문서.
> 순서대로 읽으면 "공공데이터가 어떻게 AI 답변의 근거가 되는지" 흐름이 잡히도록 배치했다.
> 최종 갱신: 2026-08-19

---

## 0. 큰 그림 — 두 개의 플로우가 있다

PRAFTA AI는 **완전히 다른 두 개의 흐름**으로 구성된다. 이 둘을 분리해서 보는 게 첫 단추다.

```
[A. 적재 플로우 · 오프라인 배치]        [B. 질의 플로우 · 실시간]
   공공데이터 원본                          사용자 질의
      → 라이선스 게이트                        ("탱크 내부 청소 작업")
      → 전처리·마스킹·청킹                        │
      → 임베딩(BGE-m3)                            → 임베딩(BGE-m3)
      → pgvector 적재                              → 벡터 검색  ← 여기서 만난다
      → HNSW 인덱스                                → 유사도 필터
           │                                       → LLM 생성(HCX-005)
           └──────── 검색 대상 제공 ──────────────┘
```

- **A는 미리, 한 번** 돌린다(수십 분짜리 배치). 자료가 갱신될 때만 다시 돈다.
- **B는 사용자가 버튼 누를 때마다** 돈다(수 초).
- **핵심 제약**: 양쪽 모두 **같은 BGE-m3로 임베딩**해야 한다. A에서 만든 좌표계 위에서 B의 질의 벡터가 이웃을 찾는 구조라, 모델을 바꾸면 코퍼스 전체를 재임베딩해야 한다.

---

## 1. 검색의 기초 — 임베딩과 벡터

### 임베딩 (Embedding)
**문장을 숫자 목록(벡터)으로 바꾸는 것.** 핵심 성질은 "의미가 비슷한 문장은 가까운 숫자가 된다"는 것.

왜 필요한가: DB의 `LIKE '%탱크%'` 검색으로는 **"탱크 내부 청소"** 와 **"밀폐공간 산소결핍 질식"** 이 관련 있다는 걸 절대 못 찾는다 — 겹치는 단어가 하나도 없으니까. 임베딩을 거치면 이 두 문장은 벡터 공간에서 가까이 놓이고, "거리"로 검색할 수 있게 된다.

### 벡터 / 1024차원
임베딩의 결과물. **숫자 1,024개짜리 배열 하나가 벡터 "하나"**이고, 그게 문장 하나를 대표한다. 배열 안의 숫자 각각은 벡터가 아니라 벡터의 **성분**이다.

수학에서 벡터는 "순서 있는 숫자 목록"이고, 이는 곧 **공간 위의 한 점**을 뜻한다.

- 숫자 2개 `[3, 4]` → 2차원 평면 위의 점
- 숫자 3개 `[3, 4, 5]` → 3차원 공간 위의 점
- 숫자 1,024개 → **1,024차원 공간 위의 점** (머리로 그릴 순 없지만 수학적으로는 동일)

즉 BGE-m3는 **문장 하나를 1,024차원 공간의 한 점으로 찍어주는 기계**이고, 의미가 비슷한 문장일수록 가까운 위치에 찍히도록 학습돼 있다.

1024라는 숫자는 우리가 정한 게 아니라 **모델(BGE-m3)이 정한 출력 규격**이다. 그래서 코드 곳곳에 1024가 박혀 있다:
- `EmbeddingClient` 의 `EMBED_DIM = 1024` (차원 다르면 에러 = 다른 모델이 섞인 사고 감지)
- pgvector 테이블의 `embedding vector(1024)` 컬럼

### 코사인 유사도 (cosine similarity)
**두 벡터가 얼마나 같은 방향을 보는가**로 유사도를 재는 방법. 점을 "원점에서 뻗은 화살표"로 보면, 두 화살표가 이루는 **각도**를 재는 것. 방향이 같으면 1에 가깝고, 직각이면 0.

- PRAFTA에서는 pgvector의 `<=>` 연산자가 코사인 **거리**를 주고, 코드에서 `score = 1 - distance` 로 뒤집어 유사도 점수로 쓴다.
- 예: "탱크 내부 청소" 질의에 "밀폐공간 질식" 사례가 score 0.62 → 채택. "지게차 후진 충돌" 사례가 0.31 → 탈락.

---

## 2. 임베딩을 만들어주는 것 — BGE-m3 와 TEI

> 한 문장 요약: **BGE-m3는 '모델'(두뇌), TEI는 그 모델을 HTTP 서버로 띄워주는 '서빙 엔진'(그릇).**

### BGE-m3
BAAI(북경 AI 연구원)가 공개한 **오픈소스 임베딩 모델**. 문장을 넣으면 1024차원 벡터가 나온다.

- 왜 이 모델: **한국어 포함 다국어 성능이 좋은 오픈소스 중 사실상 표준급** + 입력 8192 토큰까지 수용 + 무료 셀프호스팅 가능. (m3 = Multi-lingual, Multi-function, Multi-granularity)
- ★중요 제약: **질의와 코퍼스를 반드시 같은 모델로 임베딩해야 한다.** 모델을 바꾸면 코퍼스 전체 재임베딩이 필요하다.

### TEI (Text Embeddings Inference)
HuggingFace가 만든 **임베딩 전용 추론 서버**. BGE-m3는 받아보면 그냥 모델 가중치 파일 덩어리라서, 누군가 메모리에 올리고 추론을 돌리고 HTTP로 받아줘야 한다 — 그 "누군가"가 TEI다.

**★역할 분담을 정확히 하면:**

| 하는 일 | 담당 |
|---|---|
| 16문장을 한 요청으로 묶어 보내기 | 적재 스크립트 / 백엔드 (클라이언트) |
| 요청 수신, 토크나이징, 배치 구성, 큐잉, 초과분 자르기 | **TEI** (서버) |
| **문장 → 1024차원 벡터 계산** | **BGE-m3** (모델) |
| 벡터 응답 반환 | TEI |

그래서 "TEI로 임베딩한다"는 말은 **"TEI에 요청을 보내면 그 안의 BGE-m3가 계산해준다"**는 뜻이다.

- Docker로 `--model-id BAAI/bge-m3` 만 주면 알아서 모델을 내려받아 `/embed` 엔드포인트로 서빙.
- 덕분에 Spring 입장에서 임베딩은 그냥 **REST 호출 한 번**: `POST localhost:8090/embed` → `float[1024]`. 백엔드가 ML 라이브러리를 하나도 몰라도 된다.
- 선택지 비교 (PRAFTA가 ③을 고른 이유):
  1. 외부 임베딩 API(OpenAI 등) — 호출당 과금 + 안전 코퍼스가 외부로 나감
  2. Java 앱에 모델 내장 — 임베딩 생태계가 Python 중심이라 번거롭고 앱 메모리 잠식
  3. **전용 추론 서버를 옆에 띄움** ← 무료 + 데이터 안 나감

### 셀프호스팅 / 서빙
- **서빙(serving)**: 모델을 "요청하면 응답하는 서비스" 형태로 돌려놓는 것.
- **셀프호스팅**: 남의 API를 사서 쓰지 않고 우리 서버에서 직접 서빙하는 것.
- PRAFTA의 분리 전략 (설계 판단 포인트):

| | 모델 (두뇌) | 서빙 (그릇) | 비용 |
|---|---|---|---|
| 생성 | HCX-005 | 네이버 CLOVA Studio (관리형 API) | 토큰당 과금 |
| 임베딩 | BGE-m3 | **TEI (우리가 직접 띄움)** | **무료** |

큰 모델은 직접 돌리기 비싸니 사서 쓰고, 호출량이 훨씬 많은 임베딩(적재 수천 건 + 매 질의)은 셀프호스팅해 비용을 0으로 만든 구조.

### 토큰 (token)
모델이 글을 세는 단위. 대략 "단어 조각". 한국어는 보통 1글자 ≈ 1~2토큰.
- 모델마다 상한이 있다: BGE-m3 입력 8192토큰, HCX-005 출력 4096토큰.
- 과금도 토큰 단위: `tb_ai_call` 에 입출력 토큰을 기록하고 단가를 곱해 비용 계산.

### `--auto-truncate` / `--max-batch-tokens` (TEI 튜닝 옵션)
- `--auto-truncate`: 8192토큰 넘는 입력이 오면 **에러(413) 대신 잘라서 처리**. 우리 청크는 ≤1200토큰이라 평소엔 안 걸리는 안전벨트.
- `--max-batch-tokens 4096`: TEI가 **한 번의 계산에** 몇 토큰까지 밀어 넣을지. → §4-4 트러블슈팅 참조.

---

## 3. 벡터를 저장하고 찾는 것 — 벡터 DB, pgvector, HNSW

### "벡터 DB"란 제품명이 아니다
**"벡터 검색을 잘하는 DB"라는 기능상의 분류**이고, 그 역할을 하는 방법이 두 갈래다.

**① 전문 벡터 DB** — 벡터 검색만을 위해 만들어진 독립 제품

| 제품 | 특징 |
|---|---|
| Pinecone | 클라우드 관리형(SaaS) |
| Milvus | 오픈소스, 수억~수십억 벡터 대규모 특화 |
| Qdrant / Weaviate / Chroma | 오픈소스 |

**② 기존 범용 DB에 벡터 기능을 얹기** ← PRAFTA

| 기반 DB | 벡터 기능 |
|---|---|
| **PostgreSQL** | **pgvector 확장** ← PRAFTA |
| MySQL | 9.0부터 VECTOR 타입 |
| Oracle | 23ai부터 AI Vector Search |
| Elasticsearch, Redis | 벡터 검색 내장 |

PRAFTA 문서에서 "벡터 DB"라고 부른 건 별도 제품이 아니라 **"Postgres + pgvector가 벡터 DB 역할을 하고 있다"**는 뜻이다.

### pgvector
PostgreSQL에 **벡터 타입과 벡터 검색 기능을 추가해주는 확장(extension)**. 앱에 넣는 라이브러리가 아니라 **DB 서버 안에 설치되는 플러그인**이다(게임 본체에 DLC를 까는 것과 비슷).

설치하면 Postgres가 원래 못 하던 세 가지를 하게 된다:
1. **`vector` 컬럼 타입** — `embedding vector(1024)`
2. **거리 연산자** — `<=>` (코사인 거리) 등
3. **벡터 전용 인덱스** — HNSW

PRAFTA는 Docker 이미지 `pgvector/pgvector:pg16`(= pgvector가 미리 깔린 Postgres 16)을 쓰고, `init.sql` 에서 `CREATE EXTENSION vector;` 한 줄로 활성화한다.

**★Spring 쪽에는 pgvector 전용 라이브러리가 하나도 없다.** 평범한 `JdbcTemplate` + PostgreSQL JDBC 드라이버로 SQL을 날릴 뿐이고, 벡터는 `"[0.1,0.2,...]"` 문자열로 바인딩하고 `?::vector` 로 캐스팅한다.

**왜 전문 벡터 DB 대신 이걸 골랐나 (기술 선택의 근거):**
1. 코퍼스 규모가 수천 건이라 전문 벡터 DB의 강점(수억 벡터)이 불필요
2. 익숙한 RDB 운영 지식·SQL·백업 체계 재사용
3. **★벡터 유사도 정렬 + 일반 필터 + 조인을 한 SQL로 처리 가능** — 실제 검색 쿼리에 `ORDER BY embedding <=>` 옆에 `WHERE use_yn='Y' AND domain_tag=...` 필터와 출처 테이블 `LEFT JOIN`이 함께 들어있다. 전문 벡터 DB였으면 이걸 별도 구현해야 했다.

### HNSW 인덱스
벡터 검색을 빠르게 하는 인덱스 알고리즘 (Hierarchical Navigable Small World).

**인덱스 없이 검색하면** 질의 벡터를 코퍼스 6천 건 **전부와 하나하나 비교**해야 한다(풀스캔). HNSW는 벡터들을 **"비슷한 것끼리 연결한 지도"**로 만들어놔서, 아무 데서나 출발해 "이것보다 더 비슷한 게 옆에 있나?" 하며 몇 번 점프하면 근처에 도착한다. 내비게이션이 전국 모든 길을 계산하지 않고 큰길 → 작은길로 좁혀 들어가는 것과 같다.

**대신 근사(approximate) 검색이다** — 지름길로 가는 대가로 진짜 1등을 놓칠 수도 있다. RAG 용도에선 "비슷한 것 5개"면 충분하므로 **정확도를 조금 내주고 속도를 크게 얻는 거래**.

### 코퍼스 (corpus) — 인덱스와 별개다
**검색 대상이 되는 자료 뭉치 그 자체.** 특정 단계를 가리키는 말이 아니다.

- **코퍼스 = 장서**(도서관이 보유한 책들)
- **인덱스 = 색인 카드**(빨리 찾기 위한 부속물)

**색인 카드를 전부 태워도 장서는 그대로 남는다.** 검색도 여전히 된다 — 느릴 뿐. 실제로 대량 적재 시 인덱스를 지웠다 재생성하는데, 그 사이에도 코퍼스는 존재한다.

| 어디서 쓰이나 | 무엇을 가리키나 |
|---|---|
| `corpus/` 디렉토리 | 원본~가공물까지 **재료 창고** 전체 |
| `tb_ai_corpus_chunk` 테이블 | **DB에 들어가 검색 가능해진 상태** ← 보통 "코퍼스"는 이걸 뜻함 |
| "코퍼스 근거부재" 로그 | 검색했는데 쓸 만한 근거가 없음 |

### 청크 / 청킹 (chunk / chunking)
문서를 **검색 단위 하나로 확정하는 것**. "긴 문서 자르기"라기보다 "무엇을 벡터 하나로 만들 것인가"를 정하는 일이다.

왜 자르나: 300페이지 사고사례집을 벡터 하나로 만들면 추락·질식·감전이 다 뭉뚱그려져 **"산업안전 일반"이라는 밍밍한 좌표 하나**가 된다. 그러면 어떤 질의로도 안 걸린다. 사례 1건 단위로 잘라야 각 좌표가 선명해진다.

- `tb_ai_corpus_chunk` **한 행 = 청크 하나 = 벡터 하나**, 청크당 ≤1200토큰
- 검색 결과 "top-K 5건" = **청크 5개**
- ★**원본이 구조화돼 있을수록 청킹이 쉽다**: 시설물 사고사례는 원본이 1행=1사례라 **472행 → 472청크로 1:1**. 반면 PDF 자료는 표 괘선을 읽어 셀을 복원하고 병합셀을 forward-fill한 뒤에야 332청크가 됐다.

### top-K
검색 결과를 **상위 K건만** 가져오는 것. 크면 근거는 풍부하지만 토큰(비용)과 잡음이 늘어난다.
- PRAFTA: 기본 5건, 상한 20건 서버 강제 클램프. LLM 답변용은 6건.

---

## 4. 코퍼스 만들기 — 적재 플로우 (A)

### 4-1. 레지스트리 게이트 — "판정은 사람, 실행은 코드"

공공데이터라도 라이선스(변경금지 등)가 제각각이다. 그래서 **사람이 `sources.xlsx` 에 라이선스를 판정해 `license_checked=TRUE` 로 확정한 자료만** 파이프라인이 처리한다.

**★게이트가 라이선스를 판정하는 게 아니다.** 사람의 판정 결과를 확인하는 **검문소**다. 라이선스 해석 같은 법적 판단을 자동화하지 않겠다는 의도적 설계.

- 함께 검사하는 것: 필수 필드 누락, 원본 파일 존재, track↔라이선스 정합, 신뢰등급 누락
- **검사·리포트만 하고 데이터는 변경하지 않는다.**

### 4-2. 전처리 — 원본은 그대로 넣을 수 없다

**원본(국토안전관리원 시설물 사고사례)**
```json
{
  "accdntNm": "안양 박달 우회도로 램프 C교 P1교각 파괴사고",
  "facilAddr": "안양시 만안구 안양2동",              ← 상세주소
  "facilKindNm": "도로-교량",
  "accdntCauseDetail": "- 직접적인 원인\r\n  ㆍ코핑이...",   ← 원인
  "accdntContent": "1997. 7. 23. 경기도 안양시 박달동~경수산업도로에...",  ← 사고내용
  "deathCnt": "0", "woundCnt": "0", ...
}
```

필드가 17개로 흩어져 있는데, **임베딩은 "텍스트 하나 → 벡터 하나"**라서 무엇을 검색 대상으로 삼을지 먼저 정해야 한다. 사고명만? 원인만? 그럼 나머지 정보는 검색에서 사라진다.

**전처리 결과**
```json
{
  "chunk_id": "15053339_83070937368740f9",
  "content": "1997. 7. 23. 경기도 안양시~경수산업도로에 위치하고 있는 박달로...
              ...\n\n- 직접적인 원인\nㆍ코핑이 노면의...",
  "hazard_text": null,  "measure_text": null,
  "domain_tag": "도로-교량",
  "meta_json": { "facilAddr": "안양시 만안구", "deathCnt": "0", ... },
  "source_locator": "안양 박달 우회도로 램프 C교 P1교각 파괴사고|19970723|...",
  "track": "recompose",  "data_reliability": "집계형"
}
```

**전처리가 하는 일 5가지:**

**① 필드 결합** — 사고내용 + 원인을 **하나의 CONTENT로 합친다.** 순서도 레지스트리 지정대로("무슨 일이 있었고 → 왜 그랬는지").
★**CONTENT = 임베딩 대상 = 검색되는 것.** 여기 안 들어간 정보는 벡터 검색에 안 잡힌다.

**② 텍스트 정규화** — `\r\n` → `\n` 통일, 이중 공백 정리. 사소해 보이지만 **같은 내용이 개행만 달라도 다른 해시 = 다른 ID**가 되어 중복 적재를 유발한다(멱등성의 전제).

**③ PII 마스킹** — 실제로 작동하는 걸 확인:
- 본문: `경기도 안양시 박달동~경수산업도로` → **`경기도 안양시~경수산업도로`**
- 메타: `안양시 만안구 안양2동` → **`안양시 만안구`**

동(洞) 이하만 사라지고 시군구는 남는다. **삭제가 아니라 절단·치환**인 이유는 ⑴ "안양 지역 교량 사고"라는 지역 위험패턴 검색을 살리고 ⑵ 업체명·성명은 `○○건설`·`○○○`로 바꿔 **문장 구조를 보존**해야 임베딩 맥락이 유지되기 때문.

정규식 경계 처리도 신경 씀: `X건설` 뒤에 한글이 안 올 때만 매칭해서 **"대우건설"(회사)은 잡고 "건설현장·산업안전"(일반어)은 안 건드린다.**

**④ 필드를 용도별로 분류** — 이게 의외로 중요하다:

| 목적지 | 무엇이 | 쓰임 |
|---|---|---|
| `content` | 사고내용 + 원인 | **임베딩 대상** = 검색되는 본문 |
| `domain_tag` | "도로-교량" | 검색 시 **필터** 조건 |
| `meta_json` | 사망자수·재산피해·시설명 | **감사·표시용, LLM에 안 넣음** |
| `source_locator` | 사고명\|일자\|시설명 | 출처 역추적 |

`exclude_from_llm` 지정 필드는 메타에 보관만 하고 **LLM 프롬프트에는 안 들어간다**(토큰 절약 + 불필요한 정보로 LLM을 흔들지 않기).

`hazard_text`/`measure_text`가 `null`인 것도 의도된 결과 — 이 자료엔 유해요인 전용 컬럼이 없고, **사고 경위에서 억지로 도출하지 않기로 결정**했다(환각·오분류 회피). **"AI를 안 쓰기로 한 결정"**.

**⑤ 결정적 ID 부여 + 소스 메타 전파** — §4-3 참조.

### 4-3. 결정적 ID (content hash) — 멱등성의 근거

`chunk_id = {데이터셋ID}_{sha256(SOURCE_ID|locator|CONTENT)[:16]}`

**순번(1,2,3...)을 쓰면** 재실행할 때마다 같은 데이터가 새 번호를 받아 **중복이 쌓인다.**
**내용 해시를 쓰면** 같은 내용은 항상 같은 ID → **덮어쓰기**가 되어 중복이 원천적으로 불가능하다.

이게 **멱등(idempotent)** — "몇 번을 다시 돌려도 결과가 같다"는 성질. 자료가 버전 갱신되면 **바뀐 행만 자연히 신규 반영**되는 것도 덤.

### 4-4. 임베딩 + 적재 — 배치 크기가 두 개인 이유

```
청크 6,000개 (텍스트)
    ├─ [1단계] 임베딩 ─── TEI에 16개씩 요청  ← CPU 무거움, 건당 수백ms, 총 수십 분
    ▼
벡터 6,000개
    ├─ [2단계] 적재 ───── Postgres에 200행씩 UPSERT  ← 가볍다, 그냥 디스크에 쓰기
    ▼  DB 저장 완료
```

**★무거운 건 DB 적재가 아니라 임베딩이다.** BGE-m3의 신경망 추론을 GPU 없이 CPU로 돌려서 느린 것이고, Postgres에 6천 행 넣는 건 순식간이다.

**왜 16인가** (상반된 두 압력의 타협):
- **크게**: 1개씩 보내면 HTTP 왕복 6,000번. 16개씩이면 375번 → 통신 오버헤드 절감
- **작게**: 한 요청의 문장들은 TEI가 동시에 메모리에 올린다 → 메모리 압박 + 실패 시 재계산 손실
- 게다가 크게 보내도 TEI가 자기 예산(4096토큰) 안에서 어차피 나눠 계산하므로 이득이 없다

**왜 200인가**: DB는 정반대 성질 — 한 번에 몇 행이 들어가든 부담이 비슷해서 작게 잡으면 왕복만 늘어 손해. 다만 키우면 커밋 주기가 길어져 중단 시 손실이 커진다.

**★배치는 두 층에 따로 존재한다:**
- `EMBED_BATCH = 16` — **클라이언트가** 한 요청에 몇 문장을 담을지
- `--max-batch-tokens 4096` — **TEI가** 한 번의 계산에 몇 토큰까지 밀어 넣을지

### 4-5. 중단 복구 3종 세트 — "40분짜리 배치는 반드시 죽는다"

**도서관 비유**: 책 6,000권을 등록하는 일. 책마다 요약 카드를 쓰고(임베딩, 오래 걸림) 서가에 꽂는다(DB 적재, 금방). 3시간 뒤 정전이 났다면?

| 장치 | 하는 일 | 없으면 |
|---|---|---|
| **200행마다 커밋** | 200권 모이면 그때그때 서가에 꽂기 | 3시간치가 통째로 사라짐 |
| **resume** | 이미 꽂힌 책은 **요약조차 다시 안 씀** | 재실행 시 시간이 두 배 |
| **UPSERT + 결정적 ID** | 책 번호를 내용으로 정해 같은 책은 같은 자리 | 재실행할수록 중복이 쌓임 |

resume의 포인트는 **"넣지 않는다"가 아니라 "계산조차 하지 않는다"** — 비싼 건 DB 저장이 아니라 임베딩이니까. (`--force` 플래그로 무시하고 전량 재적재 가능)

**셋이 다 있어야 "에러 나면? 그냥 다시 돌리면 됨"이 성립한다.** 하나만 빠져도 무너진다.

**방어 장치들**: TEI 실패 시 3회 재시도 + 점증 백오프(1.5초→3.0초), 타임아웃 120초, VARCHAR 한도 초과분 절단(`clip`), **임베딩 차원 검증(1024 아니면 즉시 중단)** — 잘못된 차원이 섞이면 검색이 조용히 망가지므로 fail-fast. 백엔드 질의 쪽에도 같은 가드가 있어 **입구·출구 양쪽 방어**.

### 4-6. TEI OOM 트러블슈팅 (인프라 튜닝 실사례)

> **문제**: TEI를 CPU·WSL2(가용 ~4.8GB) 환경에 기동하자 워밍업 단계에서 메모리 초과로 프로세스가 죽고, Docker 재시작 정책에 의해 **무한 재시작 루프**. 컨테이너는 살아있는 것처럼 보이나 임베딩 요청이 전부 실패.
> **원인**: `--max-batch-tokens` 기본값(16384)이 해당 환경 가용 메모리 대비 과도. TEI가 **워밍업 시 최대 배치 크기만큼 메모리를 선점**하려 함 — 실제 요청이 없어도 기동 직후 최대치를 요구.
> **해결**: 실제 코퍼스 청크 크기(≤1200토큰)를 근거로 **4096으로 하향**. 한 배치에 청크 3개가 들어가 처리량 손실 없이 안정화. 초과 입력은 `--auto-truncate` 로 방어.

포인트: "값을 내렸더니 됐다"가 아니라 **증상 → 원인 규명 → 근거 있는 값 선정 → 부작용 방어**의 완결된 서사.

### 4-7. HNSW 사후필터 기아 (더 까다로운 튜닝 사례)

검색은 두 단계로 돈다: **① HNSW가 비슷한 후보를 뽑고(기본 40개) → ② 조건에 맞는 것만 거른다.**

**문제**: 뽑아온 40개 중에 조건(예: `track='verbatim'`)에 맞는 게 하나도 없으면 **결과 0건**이 된다. DB에는 분명히 있는데 유사도 순위가 41등이라 후보에 못 들어왔을 뿐인데.

**서점 비유**: "이 책과 비슷한 책 중 **중고책만** 주세요" → 점원이 비슷한 책 40권을 가져왔는데 전부 새 책 → **"중고책 없습니다"**. 창고에는 있는데도.

자료가 적은 종류(소수 트랙)일수록 심하다.

**왜 발견이 어려운가**: **에러가 안 난다.** 죽지도 않고 로그에 빨간 줄도 없다. 조용히 "0건"을 돌려줄 뿐인데, 0건은 "정말 없어서 0건"과 구분이 안 된다. **아무 신호도 안 주는 버그**.

**해결**: pgvector의 **iterative scan** — "조건에 맞는 게 필요한 개수만큼 나올 때까지 계속 더 뽑는다"로 동작을 바꿈.
```sql
ALTER DATABASE prafta_ai SET hnsw.iterative_scan = relaxed_order;
```
운영 DB에 적용 + `init.sql` 에도 기록(볼륨 재생성 시 유실 방지).

**덤 — 인덱스는 마지막에**: 책 꽂을 때마다 지도를 갱신하면 매번 재계산이라 느리다. 대량 적재 시엔 **인덱스 DROP → 적재 → 재생성**이 빠르다.

### 4-8. ⚠️ 발견 사항 — 마스킹 정규식 오탐 (미수정, 기록만)

```
원본: 콘크리트 타설시 도면에 대한 재검토 미비
결과: 콘크리트 타설시에 대한 재검토 미비      ← "도면" 삭제됨
```
주소 절단 패턴이 `타설시`를 시군구로, `도면`을 읍면동으로 오인해 "주소"로 판단하고 뒤를 잘랐다.
개인정보가 새는 방향(과소 마스킹)이 아니라 **본문이 깎이는 방향(과잉 마스킹)**이라 위험도는 낮다.
상세·수정방향은 `prafta-ai-stack/corpus/DECISIONS.md` "발견 사항" 절 참조.

---

## 5. RAG — 질의 플로우 (B)

### 5-1. 왜 RAG인가 — 선택지 세 개

목표: "KOSHA 사고 사례를 근거로 위험성평가를 도와주는 AI"

| 방법 | 문제 |
|---|---|
| ① 그냥 LLM에 물어보기 | 우리 자료를 모름. 모르면 지어냄(환각). **출처를 댈 수 없음** |
| ② 파인튜닝(모델 재학습) | 비쌈 + 자료 갱신마다 재학습 + **지식이 모델에 녹아 근거 추적 불가** |
| ③ **RAG** ← 채택 | 모델은 그대로, 물어볼 때마다 근거를 찾아 함께 건넴 |

**RAG의 결정적 장점**: 자료가 바뀌면 DB만 갈아끼우면 되고, **어느 자료를 근거로 썼는지 정확히 추적**된다. (면접에서 "왜 파인튜닝 안 했나?" → "코퍼스가 계속 갱신되는 도메인이라 재학습 비용 대비 RAG가 유리하고, 근거 추적도 RAG만 가능하다")

### 5-2. 이름 그대로 세 단계

**R**etrieval(검색) → **A**ugmented(증강) → **G**eneration(생성)

| 단계 | 무엇을 | 담당 |
|---|---|---|
| **R** 검색 | 질의와 비슷한 청크 찾기 + 0.40 미만 탈락 | pgvector |
| **A** 증강 | 찾은 청크를 **프롬프트로 조립** | **우리 서버 코드** |
| **G** 생성 | 그 프롬프트로 답변 만들기 | HCX-005 |

**★가운데 A가 RAG의 진짜 실체다.** 검색은 그냥 검색이고 생성은 그냥 LLM인데, **"검색 결과를 어떤 모양으로 LLM에게 보여줄 것인가"**가 RAG를 RAG로 만든다.

### 5-3. A 단계 — 실제로 조립되는 프롬프트

```
[근거 청크]
[1] (출처: 시설물 사고사례 / 신뢰등급: 집계형)
사고개요: 1997.7.23. 경기도 안양시~ 램프C교의 P1 교각이 구조적 결함으로...
감소대책: 설계변경 시 구조안전검토 실시...

[2] (출처: SIF 아카이브 / 신뢰등급: 규범형)
사고개요: 탱크 내부 청소 중 산소결핍으로...
유해요인: 밀폐공간 산소결핍
감소대책: 작업 전 산소농도 측정, 감시인 배치...

[위험성평가 요청] 탱크 내부 청소 작업
```

함께 가는 시스템 프롬프트 요약:
> "위 **[근거 청크]에 있는 내용만으로** 유해요인과 개선안을 도출해라.
> 청크에 없는 사실·수치를 **지어내지 마라.**
> 각 항목 끝에 근거 번호를 **[n] 형식으로 표기해라.**
> 근거가 전부 무관하면 **`ABSTAIN: Y`만 출력해라.**
> **신뢰등급이 '자율신고형'이면 단정하지 말고 약하게 인용해라.**"

특징 셋: **번호를 매기고**(추적용), **출처와 신뢰등급을 함께 주고**(자료의 무게를 다르게 다루도록), **포기할 길을 열어준다.**

### 5-4. 환각 억제 4중 장치

**① 그라운딩 임계값 0.40** — 벡터 검색은 **항상** "가장 가까운 것"을 돌려준다. 진짜 관련 자료가 없어도 뭔가는 나온다. 그 억지 근거를 LLM에 주면 오히려 이상한 답의 재료가 된다. 그래서 유사도 0.40 미만은 근거 채택 자체를 거부.
- 고정 진리값이 아니라 **운영 로그를 보고 조정하는 튜닝값**(환경변수). 너무 높으면 정상 질의도 근거부재로 빠지고, 너무 낮으면 필터가 무의미.

**② 각주를 LLM에게 맡기지 않는다 (제일 중요)**
LLM이 "근거는 [3]번"이라 답해도 **[3]번이 실제로 없을 수 있다**(번호 환각). 그래서 서버가 LLM이 단 번호를 실제 청크 목록과 **대조**한다:
- 실재하는 번호 → 출처 정보로 조립해 화면 표시
- 없는 번호 → **각주를 통째로 제거**

★**최종 화면의 출처는 LLM이 만든 게 아니라 서버가 검증해 만든 것.** LLM은 "몇 번을 썼다"고 주장할 뿐이고, 진위 판정은 코드가 쥔다.

**③ ABSTAIN — 못 하겠다고 말할 출구**
`ABSTAIN: Y`를 출력 형식에 명시. **"모르면 모른다고 해라"를 형식으로 강제**한 것. 그냥 "지어내지 마"라고만 하면 LLM은 대체로 뭔가를 지어낸다 — 포기할 방법이 없으니까.

**④ 3단계 폴백 — 실패해도 빈 화면을 주지 않는다**
```
1차: 근거 기반 생성 시도
  ↓ 응답 형식이 깨짐
2차: 같은 프롬프트로 1회 재시도
  ↓ 또 실패 / "근거 무관" 판정
3차: 자유생성 (근거 없이, 각주 없이, abstained=true 표시와 함께)
```
사용자에게 뭐라도 주되, **"이건 근거 없는 답"이라는 표시를 반드시 붙인다.**

### 5-5. ★JSON을 포기한 이야기 (가장 실전적인 교훈)

원래 LLM에게 JSON으로 답하라고 했으나 **HCX-005가 중괄호를 상습적으로 안 닫았다**(배열 미폐합 등). 파싱이 계속 실패.

그래서 JSON을 버리고 **줄 단위 프로토콜**로 전환:
```
ABSTAIN: N
H: 밀폐공간 산소결핍 위험 [2]
M: 작업 전 산소농도 측정 [2]
M: 감시인 배치 [2]
H: 추락 위험 [1]
M: 안전대 착용 [1]
```
`H:`=유해요인, `M:`=개선안. **괄호가 없으니 괄호를 틀릴 일이 없고**, 한 줄이 깨져도 그 줄만 버리면 된다.

> 교훈: **LLM에게 복잡한 구조를 요구하지 말고, 틀려도 복구 가능한 단순한 형식을 쓴다.**

### 5-6. 튜닝 근거를 로그로 남긴다
```
RAG 그라운딩 판정 - 질의길이=12, 후보=6건, 최상위점수=0.5231, 임계값=0.40, 채택=3건
```
최상위 점수 분포를 쌓아두면 "임계값이 너무 높아 정상 질의가 근거부재로 빠지는지" 판단할 수 있다. **튜닝을 감이 아니라 데이터로** 하기 위한 장치.

### 5-7. 출처 거버넌스 (citation / verbatim / 신뢰등급)
- **citation**: 답변에 "이 근거는 어느 기관의 어느 자료"(기관명·원문링크·라이선스)를 붙이는 것.
- **track**: 청크의 사용 방식. `verbatim`(변경금지 라이선스, 원문 그대로만 인용) 트랙은 `modifiable=false` 로 내려가 LLM이 고쳐 쓰지 못한다.
  - ★**구조적 격리**: verbatim 원문은 생성 호출에 **아예 투입하지 않는다.** 대신 "번호만 고르는 전용 호출"에만 보여주고 서버가 숫자 외 출력을 전부 폐기 → **LLM 출력물에 원문이 한 글자도 포함될 수 없어** 원문 변형 배포가 구조적으로 불가능.
- **신뢰등급**: 규범형(법규·기준) / 집계형(통계) / 자율신고형(신고 사례). 검색 필터로도 쓰고, 프롬프트에도 실어 LLM이 인용 강도를 조절하게 한다.

---

## 6. 생성 담당 — LLM 쪽 용어

### LLM (Large Language Model)
글을 읽고 쓰는 대형 언어 모델.

### HCX-005 / CLOVA Studio
- **HCX-005 = LLM 그 자체.** 네이버 HyperCLOVA X의 모델 등급명.
- **CLOVA Studio** = 네이버가 그 모델을 서빙해주는 관리형 API. (임베딩 쪽의 TEI에 대응)
- 즉 **"HCX-005에 삽입된 LLM"** 같은 건 없다. 모델은 하나뿐이고, 그걸 실행해주는 서비스가 CLOVA Studio.
- 모델명은 설정값이라 티어 교체 가능.

### VLM / Vision / 멀티모달
- **VLM은 별도 모델이 아니라 HCX-005가 가진 능력**이다. 텍스트만 주면 LLM처럼, 이미지를 함께 주면 이미지를 "보고" 답한다.
- "LLM이냐 VLM이냐"는 **다른 모델이 아니라 같은 모델의 어떤 능력을 쓰느냐**의 표현.
- 실제로 클라이언트도 하나(`LlmAnswerClient`)이고, 요청에 이미지 첨부를 넣느냐만 다르다.

| 기능 | 입력 | 부르는 이름 | 근거 조달 방식 |
|---|---|---|---|
| 위험성평가 도출 | 텍스트(질의 + RAG 근거) | LLM 사용 | **RAG 경로** — 벡터 검색으로 근거 조달 |
| TBM 교육자료 분석 | 텍스트 + **이미지/PDF 페이지** | VLM 사용 | **VLM 경로** — 첨부 이미지 자체가 근거 |

두 경로 모두 같은 HCX-005에 도착하므로 **토큰 쿼터·호출 로그를 공유**한다.

**VLM 경로의 제약 관리(실무 포인트)**:
- 요청당 이미지 ≤5 (HCX 제약) → PDF 페이지 클램프
- 긴 변 2240px 한도 → 보수적으로 2000px 다운스케일
- **디코딩 폭탄 방어**: 픽셀 수를 헤더로만 먼저 확인, 60MP 초과면 디코드 없이 거부 (악성 초대형 이미지로 서버 메모리를 터뜨리는 공격 차단)
- PDF **스트라이드 샘플링**: 전 페이지가 아니라 5페이지 간격으로 최대 10페이지 → 토큰(비용) 통제
- 환각 억제는 RAG 대신 프롬프트로: **"첨부에서 실제로 보이는 내용만 근거로 서술하라"**

### 프롬프트 / 시스템 프롬프트
- **프롬프트**: LLM에 넣는 입력 전체.
- **시스템 프롬프트**: "너는 누구고 어떻게 답해야 한다"는 역할·규칙 지시부. 사용자 입력과 분리 관리.
- PRAFTA는 용도별 4종 분리: grounded 생성 / free 생성 / verbatim 선택 / 이미지 확인.
- 버전 이력: 위험성평가 v3.x → **v3.11**, TBM **v4**. 실패 사례를 보고 지시문을 고쳐온 반복의 기록.

### 샘플링 파라미터 (temperature, top-p, top-k, repetition penalty)
LLM이 다음 단어를 고를 때의 "무작위성 조절 손잡이".
- **temperature**: 낮으면 보수적·일관적, 높으면 창의적. PRAFTA는 **0.2로 낮게** — 근거 기반 답변은 매번 비슷하게 나오는 결정성이 중요.
- **top-p / top-k**: 후보 단어를 확률 상위 몇 %/몇 개로 제한.
- **repetition penalty**: 같은 말 반복 억제.
- 전부 환경변수 오버라이드 → **코드 수정·재배포 없이 조정**.

---

## 7. 운영(Ops) 쪽 용어

### 토큰 쿼터 / 호출 감사 로그
- **쿼터**: 회사(테넌트)별 **월 토큰 한도**(80만, 특정사 무제한). AI를 무한정 쓰면 CLOVA 과금이 무한정 나가므로 SaaS에서 테넌트별 비용 통제는 필수.
- **감사 로그**(`tb_ai_call`): 호출 1건마다 누가·어느 기능이·토큰 몇 개·비용 얼마·어떤 근거 청크로·abstain 여부까지 기록. **비용 정산과 품질 분석(임계값 튜닝)의 원천 데이터.**
- ★재시도·폴백으로 LLM을 여러 번 부르면 **호출 건마다 기록**한다(누락 없음).

### PII-aware 관측성
로그는 남기되 **개인정보는 안 남기는** 설계. 질의 원문은 저장하지 않고 **길이만** 기록. 해시조차 역추적 리스크로 배제. "관측성과 프라이버시의 트레이드오프를 설계 단계에서 처리했다".

### 기능 게이트 (feature gate)
`prafta.ai.llm.enabled` 기본 OFF. OFF면 LLM 클라이언트 **빈(bean) 자체가 안 만들어지고** 엔드포인트는 명시적 에러코드(`AI_503_001`)로 답한다. 키 없이도 서버가 정상 기동하고, 켜고 끄는 게 배포 없이 환경변수로 된다.

### 비동기 처리 / 부분 실패 격리
TBM 자료 10건 분석에 사용자를 몇 분씩 기다리게 할 수 없으므로 **저장 즉시 응답하고 분석은 백그라운드**(`@Async`). 항목마다 즉시 커밋해 **3번째가 실패해도 1·2번째 성공분은 살아있다.** "AI 호출은 느리고 가끔 실패한다"를 트랜잭션 설계에 반영.

### 멱등 (idempotent)
같은 작업을 **몇 번 다시 해도 결과가 같은** 성질. → §4-3, §4-5

### 라이선스 레지스트리 게이트
→ §4-1 ("판정은 사람, 실행은 코드")

---

## 8. 마지막으로 — MLOps vs LLMOps

- **MLOps**: ML 모델을 실제 서비스에서 **돌아가게 하고 유지하는** 운영 기술 전반. PRAFTA는 모델을 "학습"시키진 않지만, 임베딩 모델을 **서빙**하고 그 모델이 먹을 **데이터 파이프라인**을 운영하는 부분이 여기 해당.
- **LLMOps**: 그중 LLM 서비스 특화 운영 — 프롬프트 관리, 토큰 비용 통제, 환각 억제, 호출 로깅/평가, 폴백 설계. PRAFTA 경험의 무게중심은 이쪽.

한 줄 비유: **MLOps가 "주방 설비를 돌리는 기술"이라면, LLMOps는 "외부 유명 셰프(LLM)를 데려다 쓰는 식당 운영술"** — 셰프에게 뭘 보여주고(RAG·프롬프트), 재료비를 어떻게 통제하고(토큰 쿼터), 셰프가 헛소리하면 어떻게 막을지(그라운딩·각주검증·abstain)가 핵심이다.

### ⚠️ 용어 주의 — "튜닝"
면접에서 "튜닝했다"고 하면 **파인튜닝(모델 가중치 재학습)**으로 오해될 수 있다. PRAFTA는 파인튜닝을 하지 않았고, 조정한 것은 전부 **모델은 그대로 두고 주변 파라미터·프롬프트·데이터를 조정한 것**이다.

- ❌ "모델을 튜닝했다"
- ✅ "검색 임계값·top-K·샘플링 파라미터를 **운영 로그 기반으로 튜닝할 수 있게 설계**했다. 파인튜닝 없이 RAG와 프롬프트로 품질을 확보했다"

**조정 가능한 손잡이 전체 목록:**

| 계층 | 손잡이 | 현재값 |
|---|---|---|
| 검색 품질 | 유사도 임계값 / top-K / 청크 크기 | 0.40 / 5·6·20 / ≤1200토큰 |
| LLM 생성 | temperature / top-p / repetition penalty / max tokens | 0.2 / 0.8 / 1.1 / 4096 |
| 프롬프트 | 시스템 프롬프트 4종, 출력 형식 | v3.11, v4 |
| 비용·입력량 | PDF 샘플링 간격·상한 / 이미지 크기 / 출력 글자수 | 5·10 / 2000px / 1000~1500자 |
| 인프라 | TEI max-batch-tokens / 임베딩·DB 배치 / HNSW iterative_scan | 4096 / 16·200 / relaxed_order |

---

## 부록. 확정된 두 플로우 (정리본)

### A. 적재 플로우 (벡터 DB 구축)

```
1. Postgres + pgvector 확장 사용
   - 전문 벡터DB(Pinecone·Milvus 등)가 있으나 코퍼스 규모(수천 건)상 과하다고 판단
   - 벡터 유사도 정렬 + 메타 필터 + 출처 조인을 한 SQL로 처리 가능한 점이 결정적
   - 기존 MySQL과 분리된 2차 데이터소스로 운영

2. 공공데이터포털·KOSHA 등에서 원본 수집 → raw/ 에 무변경 보관
   - API(JSON)/CSV(cp949)/XLSX/PDF 4형식, 형식별 리더 구현

3. 레지스트리 게이트 — 사람이 확정한 라이선스 판정(license_checked=TRUE)이 있는
   자료만 통과. 필수필드·원본존재·라이선스 정합성도 검사하며, 검사·리포트만 하고
   데이터는 변경하지 않는다. ("판정은 사람, 실행은 코드")

4. 전처리 — 원본을 벡터DB에 넣을 수 있는 형태로 변환
   (임베딩은 텍스트 하나 → 벡터 하나이므로, 무엇을 검색 대상으로 삼을지 확정하는 단계)
   > 청킹(검색 단위 확정) : 원본이 1행=1사례라 472행→472청크로 1:1.
     PDF는 표 괘선을 읽어 행을 복원한 뒤 청크화
   > 필드 결합 : 흩어진 컬럼을 검색 단위 하나(CONTENT)로 결합
   > 텍스트 정규화 : 개행코드 등 자료별 차이를 단일 규칙으로 통일
     (같은 내용이 개행만 달라도 다른 해시=다른 ID가 되어 중복 적재 유발)
   > PII 마스킹 : 업체명·성명은 익명 치환(문장 구조 보존 → 임베딩 맥락 유지),
     주소는 시군구까지만 보존(지역 위험패턴 검색은 살림)
   > 필드 용도별 분류 : 검색 대상(CONTENT) / 필터(domain_tag) /
     감사·표시용(meta_json, LLM 미투입) / 출처 역추적(source_locator)
   > 결정적 ID 부여 : CONTENT 해시 기반 → 같은 내용이면 항상 같은 ID (멱등성의 근거)
   > 소스 메타 전파 : track(verbatim 여부)·신뢰등급을 청크까지 내려보냄

5. TEI(BGE-m3)로 각 청크를 1024차원 벡터로 변환
   - ★질의 때와 같은 모델이어야 함. 적재 때 만든 좌표계 위에서 질의 벡터가
     이웃을 찾는 구조라, 모델 교체 시 코퍼스 전체 재임베딩 필요
   - 16문장씩 요청 : 많으면 메모리 점유·재시도 손실↑, 적으면 통신 오버헤드↑
   - 차원 검증(1024 아니면 즉시 중단) — 잘못된 벡터가 섞이면 검색이 조용히 망가짐
   - TEI 실패 시 3회 재시도 + 점증 백오프

6. Postgres에 200행씩 UPSERT 적재 — 중단 복구를 전제한 설계
   - 200행마다 커밋 → 중단돼도 그 전까지는 보존
   - resume : 기적재 chunk_id는 임베딩조차 건너뜀(수십 분 재계산 방지)
   - UPSERT + 결정적 ID → 겹쳐 들어가도 중복이 아닌 덮어쓰기
   → 위 셋이 맞물려 "그냥 다시 돌리면 된다"가 성립

7. HNSW 코사인 인덱스 생성 → 질의 플로우 가능 상태
   - 대량 적재 시엔 인덱스 DROP → 적재 → 재생성이 빠름
   - ★HNSW 사후필터 기아 : 근사검색이 후보 40개만 뽑은 뒤 필터를 걸어
     소수 트랙(verbatim 등)이 DB에 있는데도 0건이 되던 문제
     → pgvector iterative_scan 으로 해결 (에러도 로그도 안 남는 유형이라 발견이 어려움)
```

### B. 질의 플로우 (RAG)

```
1. 텍스트 기반의 사용자 질의를 BGE-m3 모델을 통해서 1024배열에 담는 숫자 형태의
   벡터로 변환 (이때 BGE-m3 모델을 실행하는 서버가 TEI)

2. 변환된 벡터 정보를 가지고 pgvector가 벡터 DB에서 코사인 유사도를 기반으로
   가까운 청크를 찾는다.

3. 찾아온 청크 중 유사도 0.40 미만은 근거에서 탈락시킨다. (튜닝값)
   - 전부 탈락해 0건이면 ABSTAINED(근거 없이 자유생성) 경로로 분기

4. 살아남은 청크들을 "이 근거 안에서만 답해라"라는 시스템 프롬프트와 함께 전달하면,
   HCX-005가 그 근거를 바탕으로 최종 답변을 문장 형태로 생성한다.
   - 출력은 줄 단위 프로토콜(H:/M:), LLM이 단 [n] 각주는 서버가 실재 여부를 대조해
     검증·조립하고 없는 번호는 제거
   - 파싱 실패 시 1회 재시도 → 그래도 실패하면 자유생성 폴백
   - 호출 건마다 tb_ai_call 감사 기록 + 회사별 토큰 쿼터 차감
```
