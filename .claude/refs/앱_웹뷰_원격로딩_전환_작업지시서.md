# 앱 웹뷰 원격 로딩 전환 작업지시서 (2026-08-02 작성)

> 상태: **설계 확정 대기 · 착수 보류**
> 착수 시점: **iOS 1.0.0(116) 심사 결과가 나온 뒤**. 셸의 기동 경로를 바꾸는 작업이라
> 심사 중에 손대면 리젝 대응과 뒤엉킨다.
>
> 표기: ✋=사용자 결정/작업 필요 / 🤖=Claude 처리 가능

---

## 1. 문제 정의

### 1-1. 웹뷰를 쓰는 이유가 지금 작동하지 않는다

하이브리드(Flutter 셸 + 웹뷰 Vue) 구조를 택한 목적은
**"네이티브 수정이 아닌 변경은 서버 배포만으로 반영"** 이다.
현재 구조는 이 목적을 **전혀 달성하지 못하고 있다.**

### 1-2. 근거 (2026-08-02 실측)

| 확인 | 결과 |
|---|---|
| `lib/web_app.dart` release 경로 | `InAppLocalhostServer(documentRoot: 'assets/vue_app/')` → `http://localhost:8080/` |
| `prafta-app-frontend` 배포 스크립트 | **없음** (`prafta-web-frontend` 에만 `deploy-web.ps1` 존재) |
| `AWS_배포현황_및_운영전환가이드.md` 내 앱 프론트 언급 | **없음** |
| 로컬호스트 서빙 도입 시점 | 초기 커밋 `fb7f9e5` — 처음부터 번들 방식, 이후 재검토 없음 |

즉 **앱 프론트는 어디에도 호스팅된 적이 없다.** 웹뷰에서 현재 얻고 있는 이점은
"UI 를 Vue 로 작성할 수 있다" 뿐이고, OTA 갱신 경로는 배선되지 않았다.

### 1-3. 실제로 발생한 비용

2026-08-02, **로그인 화면 푸터 문구 한 줄**(`고객센터 1234-5678` → 이메일) 수정 때문에
Vue 재빌드 → `assets/vue_app/` 복사 → Flutter 빌드 116 생성이 필요했다.
Flutter 코드는 한 줄도 바뀌지 않았다. iOS 는 이런 변경도 **스토어 심사**를 거쳐야 반영된다.

---

## 2. 목표 / 비목표

### 목표

- Vue 만 바뀐 변경은 **웹 배포만으로** 설치된 앱에 반영된다.
- 원격 로딩이 실패해도 앱이 사용 불가 상태가 되지 않는다(번들 폴백).
- 잘못된 원격 배포를 **앱 업데이트 없이** 되돌릴 수 있다.

### 비목표 (이번 범위 아님)

- Flutter 네이티브 코드의 OTA 갱신 — 불가능하며 시도하지 않는다.
- 오프라인 전면 동작 — 로그인부터 API 가 필요해 애초에 성립하지 않는다.
- 안드로이드/iOS 서로 다른 로딩 전략 — **양 플랫폼 동일 전략**을 유지한다.

---

## 3. 현재 구조 (변경 대상)

```dart
// lib/web_app.dart initState()
if (kReleaseMode) {
  _localhost = InAppLocalhostServer(port: 8080, documentRoot: 'assets/vue_app/');
  _localhost!.start();
}

// _openInitialUrl()
final url = kReleaseMode ? 'http://localhost:8080/' : _kAppDevUrl;  // debug = LAN dev 서버
await ctl.loadUrl(urlRequest: URLRequest(url: WebUri(url)));
```

관련 설정:
- `Info.plist`: `NSAllowsLocalNetworking`(로컬 서버용), `NSAllowsArbitraryLoadsInWebContent`
- `pubspec.yaml`: `assets/vue_app/`, `assets/vue_app/assets/` (하위 폴더 각각 명시 필요)
- 2026-08-02 추가된 복구 로직(`c999620`): resume 시 로컬 서버 헬스체크·재기동,
  `onWebContentProcessDidTerminate` → reload. **원격 전환 후에도 폴백 경로에서 계속 필요하다.**

---

## 4. 목표 구조

### 4-1. 로딩 전략 — 원격 우선, 번들 폴백

```
셸 기동
  ├─ 원격 시도: https://<앱 프론트 호스트>/  (타임아웃 T초)
  │    ├─ 성공 → 원격 Vue 사용 ✅ (웹 배포만으로 갱신)
  │    └─ 실패/타임아웃 → 번들 폴백
  └─ 번들 폴백: InAppLocalhostServer('assets/vue_app/')  ← 현행 경로 그대로
```

**번들은 제거하지 않는다.** 스토어 릴리즈마다 갱신되는 "바닥"으로 남긴다.
평상시엔 쓰이지 않지만, 원격 장애 시 앱이 벽돌이 되는 것을 막는 유일한 수단이다.

### 4-2. 킬 스위치 / 버전 핀

원격 진입 전에 가벼운 매니페스트를 먼저 읽는다.

```json
// https://<host>/app-manifest.json  (CloudFront no-cache)
{
  "enabled": true,              // false → 즉시 번들 폴백 (앱 업데이트 없이 원격 중단)
  "minShellBridgeVersion": 3,   // 이 값보다 낮은 셸은 원격을 쓰지 않는다
  "entry": "/"                  // 필요 시 특정 버전 경로로 핀 (예: "/r/2026-08-02/")
}
```

- 원격 배포가 잘못됐을 때 **매니페스트만 바꿔 전 사용자를 번들로 되돌린다.**
- 매니페스트 조회 실패 시에는 **원격 시도(기존 정책)** 로 진행할지 **번들 폴백**으로 갈지
  §5 D3 에서 확정한다.

### 4-3. 브리지 버전 규약 (★이 작업의 핵심 리스크)

원격 Vue 는 최신인데 설치된 셸은 구버전일 수 있다. 이번 주에만 브리지가 2개 늘었다
(`REQUEST_CAMERA_PERMISSION` 08-01, `OPEN_APP_SETTINGS` 07-31).

**셸 → Vue 로 능력을 알린다.** `UserScript` 로 문서 시작 시점에 주입:

```js
window.__SHELL__ = {
  bridgeVersion: 3,
  platform: 'ios' | 'android',
  appVersion: '1.0.0',
  handlers: ['JS_CONSOLE','GET_GPS','GET_DEVICE_INFO','GET_APP_FOREGROUND_SEC',
             'GET_PUSH_TOKEN','SCAN_QR','OPEN_APP_SETTINGS','REQUEST_CAMERA_PERMISSION'],
};
```

**Vue 규약 (엄수)**
1. 브리지 호출 전 `handlers` 포함 여부를 확인한다.
2. 없으면 **기능을 조용히 낮추거나** "앱 업데이트가 필요해요" 로 명확히 안내한다.
   화면이 죽거나 무한 로딩에 빠지면 안 된다.
3. 새 브리지가 **필수인** 기능은 `minShellBridgeVersion` 을 올려 구셸을 번들로 보낸다.

#### 현재 브리지 목록 (v1 기준선)

| 방향 | 이름 | 래퍼 |
|---|---|---|
| Vue→네이티브 | `JS_CONSOLE` | (직접) |
| Vue→네이티브 | `GET_GPS` | `gpsBridge.js` |
| Vue→네이티브 | `GET_DEVICE_INFO` | `deviceBridge.js` |
| Vue→네이티브 | `GET_APP_FOREGROUND_SEC` | `foregroundBridge.js` |
| Vue→네이티브 | `GET_PUSH_TOKEN` | `pushTokenBridge.js` |
| Vue→네이티브 | `SCAN_QR` | (qr 호출부) |
| Vue→네이티브 | `OPEN_APP_SETTINGS` | `appSettingsBridge.js` |
| Vue→네이티브 | `REQUEST_CAMERA_PERMISSION` | `cameraPermissionBridge.js` |
| 네이티브→Vue | 푸시 토큰 refresh 콜백 | `pushTokenBridge.js` |
| 네이티브→Vue | 푸시 탭(open) 라우팅 | `pushRouteBridge.js` |

> **네이티브→Vue 방향도 계약이다.** 셸이 호출하는 `window.__on*` 콜백 이름이 원격 Vue 에서
> 사라지면 푸시 탭 라우팅이 죽는다. 이 방향은 기능 탐지가 불가능하므로 **콜백 이름은 절대
> 변경·삭제하지 않는다**(추가만 허용).

---

## 5. 착수 전 확정 필요 ✋

| ID | 결정 사항 | 선택지 / 기본안 |
|---|---|---|
| **D1** | 호스팅 위치 | (a) 기존 `prafta.com` 배포에 경로 추가(예: `/app/`) (b) 서브도메인 `app.prafta.com` 신설 — **기본안 (b)**: 관리자 웹과 캐시 정책·배포 주기를 분리할 수 있다 |
| **D2** | 원격 타임아웃 T | **기본안 3초**. 현장 네트워크가 느리면 폴백이 잦아지고, 길면 첫 화면이 지연된다. §7 T-4 로 실측 후 확정 |
| **D3** | 매니페스트 조회 실패 시 | (a) 원격 진행 (b) 번들 폴백 — **기본안 (b)**: 네트워크가 불안정한 상황에서 원격 진입은 더 나쁜 경험 |
| **D4** | 원격 성공 후 세션 중 재로딩 정책 | 앱 재기동 시에만 재판정(기본안) / 매 resume 판정 — **기본안 전자**: 사용 중 갑작스러운 소스 전환은 상태 유실 위험 |
| **D5** | 캐시 정책 | `index.html`·`app-manifest.json` = no-cache, 해시 붙은 청크 = 장기 캐시 (`deploy-web.ps1` 과 동일 원칙) |
| **D6** | 안드/iOS 동시 전환 여부 | **기본안 동시** — 분기하면 검증 매트릭스가 2배가 된다 |

---

## 6. 작업 분해

| ID | 작업 | 담당 |
|---|---|---|
| **T0** | **브리지 래퍼 7종 감사** — 핸들러 부재 시 예외 없이 우아하게 폴백하는지 전수 확인. `appSettingsBridge` 는 `isBridgeAvailable()` + `res.status` 검사로 이미 안전하나, 나머지 6종은 미확인. `callHandler('없는핸들러')` 의 실제 반환(null/예외/무응답)을 **실측**해서 문서화 | 🤖 |
| **T1** | `window.__SHELL__` 주입(UserScript, AT_DOCUMENT_START) + `bridgeVersion` 상수 도입 | 🤖 |
| **T2** | Vue 측 `shellCapability.js` 유틸 신설 — `hasHandler(name)`, `requireHandler(name, {onMissing})` | 🤖 |
| **T3** | 기존 브리지 래퍼 7종을 T2 유틸 경유로 전환 | 🤖 |
| **T4** | 셸 로딩 전략 교체 — 매니페스트 조회 → 원격/번들 판정 → 폴백. 08-02 복구 로직(헬스체크·프로세스 종료 reload)은 **폴백 경로에서 유지** | 🤖 |
| **T5** | 앱 프론트 배포 스크립트 신설 (`deploy-app-web.ps1`) — `deploy-web.ps1` 과 동일하게 **git 커밋된 코드만** 배포, 캐시 정책 D5, 배포 이력 기록 | 🤖 |
| **T6** | S3 버킷/CloudFront 배포 구성 (D1 결정 반영) | ✋🤖 |
| **T7** | ATS/보안 재점검 — 원격은 HTTPS 라 `NSAllowsArbitraryLoadsInWebContent` 축소 가능한지 검토. 번들 폴백용 `NSAllowsLocalNetworking` 은 유지 | 🤖 |
| **T8** | §7 테스트 전량 수행 | ✋🤖 |
| **T9** | 문서 갱신 — `AWS_배포현황_및_운영전환가이드.md`, `배포_운영_매뉴얼.md`, `CLAUDE.md` 의 "`assets/vue_app/` 직접 편집 금지" 항목에 원격 배포 경로 추가 | 🤖 |

---

## 7. 테스트 계획 ★

> 이 전환은 **실패 시 전 사용자가 동시에 앱을 못 쓰는** 종류의 변경이다.
> 아래는 전량 통과가 착수 완료 조건이며, 하나라도 미통과면 배포하지 않는다.
>
> 판정 원칙: **"화면이 뜨는가"가 아니라 "어느 소스로 떴는가"를 확인한다.**
> 이를 위해 T1 에서 **현재 로딩 소스를 화면에서 확인할 수단**(마이페이지 하단 빌드 정보에
> `remote/bundle` + 원격 배포 해시 표기)을 함께 넣는다. 없으면 아래 시험 절반이 판정 불가다.

### 7-1. 로딩 경로 판정 (L)

| ID | 시나리오 | 기대 결과 |
|---|---|---|
| L-1 | 정상 네트워크 + 매니페스트 `enabled:true` | **원격** 로딩. 표기 `remote` |
| L-2 | 매니페스트 `enabled:false` | **번들** 폴백. 표기 `bundle`. 앱 재설치 없이 전환됨 |
| L-3 | 원격 호스트 DNS 실패 | 번들 폴백, 에러 화면 없음 |
| L-4 | 원격 호스트 응답 지연(T 초 초과) | T 초 후 번들 폴백. **체감 지연 측정** → D2 확정 |
| L-5 | 원격 200 이지만 `index.html` 내용 손상 | 번들 폴백 또는 명확한 에러 안내. **백지 화면 금지** |
| L-6 | 원격 `index.html` 은 받았으나 청크 404 (배포 중간 상태) | 화면이 부분 동작하다 죽지 않아야 함. 폴백 또는 재시도 안내 |
| L-7 | `minShellBridgeVersion` > 셸 버전 | 번들 폴백. 표기 `bundle` |
| L-8 | 매니페스트 조회 자체 실패 | D3 결정대로 동작 |
| L-9 | 비행기 모드로 콜드 스타트 | 번들 폴백 → 로그인 화면까지는 뜸(이후 API 실패는 기존 동작) |

### 7-2. 브리지 스큐 (B) ★ 최우선

> **구셸 + 신Vue 조합이 이 작업의 최대 위험이다.** 원격 Vue 는 항상 최신인데 설치된 셸은
> 사용자가 업데이트하지 않으면 몇 달 전 버전일 수 있다.

| ID | 시나리오 | 기대 결과 |
|---|---|---|
| B-1 | 구셸(`OPEN_APP_SETTINGS` 없음) + 신Vue | 권한 폴백 화면의 [설정으로 이동]이 **무반응/무한로딩이 아니라** 수동 안내로 대체 |
| B-2 | 구셸(`REQUEST_CAMERA_PERMISSION` 없음) + 신Vue | QR 진입 시 기존 경로로 동작하거나 명확한 안내. **검은 화면 금지** |
| B-3 | 없는 핸들러 호출 시 반환값 | T0 실측 결과대로 처리되고 **예외가 화면까지 전파되지 않음** |
| B-4 | 신셸 + 구Vue(번들 폴백 상태) | 정상 동작(핸들러가 더 많은 건 문제없음) |
| B-5 | 네이티브→Vue 콜백(푸시 토큰 refresh) | 원격 Vue 에서도 콜백 이름이 살아 있어 정상 수신 |
| B-6 | 네이티브→Vue 콜백(푸시 탭 라우팅) | 앱 종료 상태에서 푸시 탭 → 해당 화면 진입 |
| B-7 | 브리지 8종 전수 호출 | 원격/번들 **양쪽 모두**에서 정상 동작 |

**구셸 준비 방법**: 현재 `main` 에서 브리지 2종(`OPEN_APP_SETTINGS`,
`REQUEST_CAMERA_PERMISSION`)의 `addJavaScriptHandler` 등록을 주석 처리한 **시험 전용 빌드**를
만들어 사용한다. 이 빌드는 **절대 배포하지 않는다**(`diag/` 브랜치 관례 준용).

### 7-3. 네트워크 조건 (N)

| ID | 시나리오 | 기대 결과 |
|---|---|---|
| N-1 | 저속(3G 수준) 콜드 스타트 | 원격 성공 또는 T 초 내 폴백. 흰 화면 방치 금지 |
| N-2 | 로딩 도중 네트워크 끊김 | 진행 중 화면 유지 또는 재시도 안내 |
| N-3 | Wi-Fi ↔ LTE 전환 중 기동 | 정상 |
| N-4 | 캡티브 포털(공용 Wi-Fi 로그인 페이지 가로채기) | 포털 HTML 을 앱 화면으로 렌더하지 않아야 함. 매니페스트 JSON 파싱 실패로 폴백 |

> N-4 는 실제로 흔한 사고 유형이다. **매니페스트를 JSON 으로 엄격 파싱**하고 실패 시
> 폴백하면 자동으로 막힌다 — 구현 시 반드시 반영할 것.

### 7-4. 라이프사이클 / 회귀 (R)

> 2026-08-02 수정분(`c999620`)과 충돌하지 않는지 확인한다.

| ID | 시나리오 | 기대 결과 |
|---|---|---|
| R-1 | 원격 상태에서 장시간 백그라운드 → 복귀 | 정상. 버튼·이미지 동작 |
| R-2 | 번들 폴백 상태에서 장시간 백그라운드 → 복귀 | 로컬 서버 헬스체크 → 재기동 → 스낵바 후 정상 |
| R-3 | 웹뷰 프로세스 강제 종료(iOS 메모리 압박 재현) | reload 후 정상 복구 |
| R-4 | 원격 상태에서 당겨서 새로고침 | 정상 |
| R-5 | 앱 재기동 반복 10회 | 매번 같은 소스로 안정적으로 진입(플래핑 없음) |
| R-6 | 관리자 모드 진입/현장 전환 | 정상 |
| R-7 | QR 스캔(순회점검·일용직 입실) | 원격/번들 양쪽 정상 |
| R-8 | 출퇴근 GPS · 외근 등록 | 원격/번들 양쪽 정상 |
| R-9 | 파일 첨부(사진 촬영·갤러리) | 원격/번들 양쪽 정상 |

### 7-5. 캐시 / 배포 (C)

| ID | 시나리오 | 기대 결과 |
|---|---|---|
| C-1 | 웹 배포 직후 앱 재기동 | **새 버전이 즉시 반영**(index.html no-cache 확인) |
| C-2 | 배포 직후 CloudFront 무효화 전 | 낡은 청크 참조로 깨지지 않음 |
| C-3 | 연속 2회 배포 | 중간 버전에 고착되지 않음 |
| C-4 | 배포 롤백(이전 커밋 재배포) | 앱에서 이전 화면으로 복귀 |
| C-5 | 오프라인 상태에서 앱 재기동(직전 원격 성공) | 번들 폴백으로 진입(원격 캐시에 의존하지 않음) |

### 7-6. 보안 (S)

| ID | 항목 | 기대 결과 |
|---|---|---|
| S-1 | 원격 로딩이 **HTTPS 전용**인지 | http:// 로는 원격 진입 불가 |
| S-2 | 원격 호스트 화이트리스트 | 매니페스트 `entry` 가 임의 외부 도메인을 가리켜도 **우리 호스트가 아니면 거부** |
| S-3 | 인증서 오류 시 | 진입 거부 → 번들 폴백. `onReceivedServerTrustAuthRequest` 가 **무조건 PROCEED 하지 않도록** 재검토 ★현행 코드는 무조건 PROCEED 다 |
| S-4 | 토큰/세션 저장 위치 | 원격 오리진 전환으로 `sessionStorage` 의 `gv_*` 클레임이 유실되지 않는지 확인 ★**오리진이 바뀌면 스토리지가 분리된다 — 로그인 유지에 직결** |
| S-5 | ATS 축소 가능 여부 | T7 결과 반영 |

> **S-3·S-4 는 설계 단계에서 반드시 다뤄야 한다.**
> 특히 S-4: 현재 `http://localhost:8080` 오리진에 저장된 세션이 원격 오리진으로 바뀌면
> **전 사용자가 재로그인**하게 된다. 전환 시 1회 재로그인을 감수할지, 마이그레이션
> 경로를 둘지 §5 에 결정 항목으로 추가해야 한다. → **D7 로 승격 필요**

### 7-7. 플랫폼 매트릭스

7-1 ~ 7-6 을 **iOS 실기기 + 안드로이드 실기기** 양쪽에서 수행한다.
시뮬레이터/에뮬레이터 결과는 참고용이며 통과 판정 근거로 쓰지 않는다
(QR 검은 화면 건에서 실기기와 결과가 갈린 전례).

### 7-8. 단계적 롤아웃

전량 통과 후에도 즉시 전면 적용하지 않는다.

1. `enabled:false` 상태로 배포 → **전원 번들** (원격 코드가 실기기에 있지만 미사용)
2. 내부 인원만 원격 강제(디버그 스위치) → 수일 사용
3. `enabled:true` 전환 → 전체 원격
4. 이상 시 `enabled:false` 로 즉시 회귀 (앱 업데이트 불필요)

---

## 8. 롤백 절차

| 상황 | 조치 | 소요 |
|---|---|---|
| 원격 Vue 에 결함 | 이전 커밋 재배포(C-4) | 수 분 |
| 원격 자체가 불안정 | 매니페스트 `enabled:false` | 즉시 |
| 매니페스트/호스트 장애 | 앱이 자동으로 번들 폴백 | 자동 |
| 셸 로딩 로직 자체가 결함 | **앱 업데이트 필요** ← 이것만 되돌릴 수 없다 |

마지막 행 때문에 §7 이 중요하다. **셸 로딩 로직은 원격으로 못 고친다.**

---

## 9. 리스크 대장

| 리스크 | 영향 | 완화 |
|---|---|---|
| 브리지 스큐로 구셸 사용자 기능 파손 | 높음 | §4-3 규약 + B 시험 전수 + `minShellBridgeVersion` |
| 오리진 변경으로 전 사용자 재로그인 | 중간 | D7 에서 확정 (S-4) |
| 원격 배포 사고가 전 사용자에 즉시 전파 | 높음 | 킬 스위치 + 단계적 롤아웃 + 번들 폴백 |
| 첫 화면 지연 | 중간 | 타임아웃 D2 실측 + CloudFront |
| Apple 4.2 "최소 기능" 지적 | 낮음 | 웹뷰 콘텐츠 원격 로딩은 허용됨. 네이티브 기능(QR·GPS·푸시) 보유 소명 유지 |
| 셸 로딩 로직 결함 = 원격 복구 불가 | 높음 | §7 전량 통과 + 단계적 롤아웃 1단계(코드는 있으나 미사용) |

---

## 10. 참고

- 현재 셸: `PRAFTA_FLUTTER/safenote/lib/web_app.dart`
- 앱 프론트: `PRAFTA/prafta-app-frontend/prafta-app-frontend/`
- 기존 웹 배포 스크립트(참고 구현): `PRAFTA/prafta-web-frontend/prafta-web-frontend/scripts/deploy-web.ps1`
- 08-02 라이프사이클 복구 로직: Flutter `c999620`
- 관련 메모리: `project_prafta_app_vite_and_api_align`,
  `feedback_qr_scanner_which_component_and_html5qrcode_css`
