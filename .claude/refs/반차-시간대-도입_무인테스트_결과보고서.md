# 반차 시간대 도입 — 무인 야간 테스트 결과 보고서

> 실행: 2026-08-08 새벽 (Claude Code 무인 실행)
> 기준 문서: `반차-시간대-도입_실기기_테스트_시나리오.md` (71 시나리오) · `반차-시간대-도입_작업요약_및_배포검증서.md`
> 방식: Playwright 실브라우저 — 웹(8081)·앱(8082) 화면의 버튼을 실제로 눌러 API 호출 → 개발 DB 기록 → MCP 로 DB 검증 → 스크린샷 증거
> 증거 폴더: `.claude/refs/무인테스트_증거/`

---

## ⚡ 요약 (아침에 이것부터)

**상태: ✅ 완료 (2026-08-08 05:0x 종료)** — 실행한 검증 **전 항목 PASS, FAIL 0건**. 신규 잔결함 후보 1건(아래 확정 필요 §1). 미실행 그룹은 §실기기·후속 잔여에 정직하게 분리했다.

| 구분 | 수 | 내용 |
|---|---|---|
| **PASS** | **28** | 경계 산출·제출·저장 규약·판정 4경로 대조·★★OT 게이트·겹침 거부·표기(F-3)·주52·잠금·승인·버튼 규약 등 |
| **FAIL** | **0** | — |
| 잔결함 후보 | 1 | OT `등록 가능` 칩 연차 면제 미반영 (동작 정상·안내 오류, F-계열) |
| 미실행(후속) | 7그룹 | 야간(A-6·C-6·D-4) / 가불(P-3) / 일용직(O) / 사업장 이동(G-4·G-6) / 구데이터 R / 하도급 / 실시간 셀프 PUSH |

**한 줄 결론: 반차 시간대의 핵심 체인(신청→저장→판정→OT→집계→표기)이 실화면·실DB에서 전부 정합. 배포본 그대로 두어도 된다.**

### 사용자 확정 필요 항목

1. ~~**[신규 잔결함 후보] OT `등록 가능` 칩 연차 면제 미반영**~~ — ✅ **2026-08-08 오전 해소·실검증 완료** (사용자 지시 "이번에 같이 잡고 가자"). 서버가 검증과 **동일 단일 출처**(`buildLeaveExemptSegments`/`exemptStampRange`)로 면제 구간을 조회 응답에 additive 로 싣고, 웹·앱 칩이 그대로 뺀다.
   - 실검증(동일 시드·동일 하네스 before/after): 웹 칩 `15:00~19:00` → **`18:00~19:00`** / 앱 칩 **`18:00~19:00`** / 회귀: 면제 없는 날(07-12) `13:00~17:00` 전량 **불변**
   - 변경: BE 6파일(신규 record 2) + 웹 FE 1 + 앱 FE 2. 증거 `FIX-ot-chip-after.png` · `FIX-app-ot-chip.png`
   - ⚠️ **앱 FE 2파일은 릴리즈 게이트** — 번들 재빌드 + 스토어 반영 필요(1.0.1 재빌드에 태울 수 있음)
2. **[확인 요망] QTOVN 시드의 휴게 불일치** — 개발 DB 야간 근무타입(00005)이 `휴게분 60 ↔ 휴게시각 23:30~24:00(30분)` 으로 서로 다름. 테스트 시드 오염인지, 실사용에서 가능한 입력인지 확인 필요. **가능하다면 경계 산출이 어느 값을 기준으로 하는지가 야간 검증(잔여 §4)의 선행 질문**이 된다
3. **[판단 요망] 미결 잠금 문구(e0b88178)의 실발동 경로 부재** — 표준 앱·웹 신청이 REQ 와 동시에 USE 를 CONFIRMED 로 만들어 확정 잠금이 항상 우선. 오늘 정정한 미결 문구는 안전망으로 존치해도 무해하나, "USE 없이 REQ 만 있는 상태"가 실제로 어떤 경로에서 생기는지 확인해 두면 좋음 (급하지 않음)

---

## 0. 사전 기록

- **권한**: `settings.local.json` allow 규칙 3건 (내일 삭제하면 원상복구)
- **스모크에서 발견·해소**: 약관 재동의 게이트(제3자 제공동의 07-28 신설 여파)가 QT 전 계정에 걸려 있었음. 하네스 `lib/browser.mjs` 에 게이트 처리 추가(웹=팝업 2단 / 앱=`#/TermsAgree` 페이지). **QTHR·QTUSERA 약관 동의가 개발 DB 에 저장됨** (테스트 부수효과, 정리 불요)
- **PUSH**: 로컬 firebase 키 없음 → 실발송 불가. P4 는 outbox 행 검증으로 대체
- **워킹트리 주의**: 로컬 백엔드는 워킹트리 기준 구동 = 미커밋 SMS 병행분 포함(게이트 OFF). 이상 발견 시 반차 결함인지 SMS 간섭인지 분리 기록
- **계약**: 운영 무접점 / 코드 수정 0·커밋 0 (솔루션만 기록) / 시드 전량 기록 + cleanup SQL

---

## 진행 로그 (실행 순, 실시간 갱신)

- [02:0x] 스모크 완료. Phase 0 (DB 사전점검) 시작
- [02:1x] **Phase 0 완료.** 실측 결과:
  - 마감: QT(00010) 202607·202608 **마감 없음** → 과거일 시드 가능
  - 사용단위 정책 = **MIN_30 (전 단위 허용)** — 최초 조회가 ORDER BY 없이 구행(FULL_DAY)을 잡아 오판했다가 정정. 최신 `POLICY_SEQ=24` 가 MIN_30. **정책 변경 불필요, cleanup 불요.** `AXIS4_PRORATE_ROUNDING='CEIL'` → 사용단위 잠금도 없음
  - 계정: QTUSERA(잔여 12.775) · QTUSERC(15.0) · QTUSERG(월차형) 신청 가능 / QTUSERB·F 는 부여 0 (결재·관리자 대행용)
  - 스케줄 기존 시드: `00001 QT8H`(09-18, 휴게 12-13 → D=480, H=240, **경계 14:00**) · `00004 QT2SEG`(2구간 09-14+18-22, 휴게0 → D=540, H=270, **경계 13:30**) · `00005 QTOVN`(야간 22:00-04:30, ★**휴게분 60 ↔ 휴게시각 23:30-24:00(30분) 불일치 시드** — 그 자체가 검증 소재)
  - 배정: QTUSERA·C 에 8월 평일 QT8H 배정 존재(~08-14). **오늘(08-08, 토)은 배정 없음** → 셀프 출근 시나리오는 당일 배정 선행 필요
- [02:2x] **H-2 PASS** — Baim_07 라디오 5개(종일/반차/2h/1h/30분), **반반차(QUARTER_DAY) 라디오 부재** 실측. 증거 `P1-baim07-before.png`
- [02:2x] Phase 2 시작 — 앱 연차 신청 화면(A·H-1·J 계열)
- [02:4x] A-1·H-1·J-1·F-5 PASS (경계 14:00 화면 정합)
- [03:0x] A-2 제출 성공(08-11) + Q1·Q5 저장 규약 DB 검증 + F-6 시트 + G-2 잠금 모달 PASS. 08-10 1차 시도는 기존 종일 연차 중복으로 서버 거부(가드 실동작 확인)
- [03:3x] 과거일 반차 4건(04·05·06 START / 07 END) 신청 + 관리자 근태 3일 입력(웹 일자상세)
- [04:0x] **판정 대조 PASS** — Attd_08/Attd_11: 05=지각1분·06=정상·07=정상·04=미출근1. **★★D-1 OT 게이트 PASS**(15~19 거부 `ATTD_400_012` / 18~19 성공 60분)
- [04:3x] Q 겹침 거부·I 표기(B안·검산 정합)·P-1 ⓘ·MyPage 값 일치 PASS
- [04:5x] F 주52(16시간 정확)·P5 앱 관리자 3일 대조·M 승인·L-1 버튼 규약 PASS. PUSH outbox 6행 확인
- [05:0x] **전 배치 종료. FAIL 0.** 보고서 확정

---

## 시나리오 결과

### 그룹 A·H·J (앱 신청 화면) — 02:3x 실측

| ID | 결과 | 실측 내용 | 증거 |
|---|---|---|---|
| **A-1** | ✅ PASS | 08-10(QT8H 09-18, 휴게 12-13) 반차 선택 → 카드 2개: 늦게 출근 `09:00~14:00 이 시간까지 쉬고 출근` / 일찍 퇴근 `14:00~18:00 이 시간부터 쉬고 퇴근` + "기준 시각은 **14:00**". **사전 계산(D=480·H=240·휴게 건너뜀)과 정확히 일치** — 서버 경계 산출이 화면까지 정합 | `A-1-halfcards-0810.png` |
| **H-1** | ✅ PASS | 사용단위 칩 5개(종일/반차/2시간/1시간/30분) — **반반차 칩 부재** | 〃 |
| **H-2** | ✅ PASS | Baim_07 라디오 5개, QUARTER_DAY 부재. 현정책 MIN_30 정상 로드(무선택 결함 없음) | `P1-baim07-before.png` |
| **J-1** | ✅ PASS | 08-08(토, 미배정) → 반차·2h·1h·30분 **전부 disabled**, 종일만 활성 + "이 날은 근무계획이 없어 종일 연차만 신청할 수 있어요." | `J1-0808-chips.png` |
| **F-5** | ✅ PASS | 미배정일 종일 선택 → "이 날은 근무계획이 없어요(휴무). 종일 연차를 써도 실제로 더 쉬게 되지는 않아요." **차단 아님**(신청하기 존치) | `F5-0808-fullday.png` |

부수 확인: 날짜 미선택 상태에선 반차 카드가 `--:-- ~ --:--` 플레이스홀더 + 칩 낙관 enable(비관 disable 아님) — 설계 의도(HB-11) 그대로.
★ 실행 노트: `?workYmd=` 쿼리 단독 진입은 날짜를 프리필하지 않는다(sessionStorage 컨텍스트와 정합할 때만 채택 — stale 방지 설계). 날짜는 폼 스테퍼로 직접 선택해야 한다.

### 그룹 A-2 제출 + Q1·Q5 저장 규약 + F-6 + G-2 — 03:0x 실측

| ID | 결과 | 실측 내용 | 증거 |
|---|---|---|---|
| **A-2** | ✅ PASS | 08-11 시작기준(늦게 출근) 반차 제출 성공. 1차 시도(08-10)는 **기존 승인 종일 연차와 중복으로 서버 거부** — 중복 점유 가드 실동작을 겸사 확인 | `A-2-after-submit.png` |
| **Q1 (REQ 시각 저장)** | ✅ PASS | `tb_user_attd_req 2026080800189`: START_TIME `0900` / END_TIME `1400` / LEAVE_DAYS 0.5 / **START_DATE=END_DATE=WORK_YMD**(단일일 고정) | DB 실측 |
| **Q5 (USE 저장 규약)** | ✅ PASS | `tb_user_leave_use LV2026080800101`: USE_UNIT_TYPE `01` / `0900~1400` / 0.5일 / **LEAVE_MINUTES 240**(D=480 절반) / CONFIRMED | DB 실측 |
| **F-6** | ✅ PASS | 결재자 시트: 오픈 중 `body overflow: hidden`(잠금) → 닫힘 후 `visible`(해제). 후보 3인 노출, 선택 → 결재선 1인 반영 | `F6-approver-sheet.png` |
| **G-2 (스케줄 잠금)** | ✅ PASS | Attd_05 에서 반차일(08-11) 셀에 QT7H 적용→저장 → **"요청 1건 중 0건 성공 / 1건 실패"** + 사유 `연차(종일/반차/시간차)가 등록된 날짜는 근무 스케줄을 변경할 수 없습니다.` 처리 결과 모달로 노출 | `G2-attd05-result.png` |
| (부수) 부분 휴가 상세 | ✅ | Attd_05 반차일 셀 배지 클릭 → 팝업에 `연차·반차·09:00~14:00·240분` — **웹면에서도 반차 시간대 정확 표시** | 콘솔 로그 |

★ **관찰 (오늘 배포 e0b88178 관련, 정보성)**: 앱 신청은 REQ(미결 `01`)와 동시에 LEAVE_USE 를 `CONFIRMED` 로 만든다(기존 설계). 따라서 잠금 대표사유는 항상 **확정 분기**(`REASON_HAS_LEAVE`)가 우선하고, 오늘 정정한 **미결 분기 문구**(`반차 또는 시간 단위 연차…`)는 표준 신청 경로에서는 확정 문구에 가려진다 — 정정문구는 안전망 성격. 문구 노출 자체는 정확했다(반차 포함 문구).

### 그룹 C·D·E 핵심 — 판정 6경로 중 웹 2면 + ★★OT 게이트 — 03:3x~04:0x 실측

**시드** (앱 화면 신청 + 웹 일자상세 관리자 직접 입력, 전부 UI 조작):

| 날짜 | 반차 | 근태(관리자 입력) | 용도 |
|---|---|---|---|
| 08-04 | START 0900~1400 확정 | 없음(무출근) | E-1 결근 |
| 08-05 | START 0900~1400 확정 | 14:01~18:00 | 지각 1분 |
| 08-06 | START 0900~1400 확정 | 14:00~18:00 | 경계 정시 |
| 08-07 | **END 1400~1800** 확정 | **2구간 09:00~14:00 + 15:00~19:00** | ★★D-1 |

| ID | 결과 | 실측 내용 | 증거 |
|---|---|---|---|
| **C-1 (경계 정시=정상)** | ✅ PASS | Attd_08: 08-06 `정상`, 지각 0분. **작업 전엔 09:00 기준 지각 300분 오판이던 형상** | `P6-attd08.png` |
| **C-2 (경계+1분=지각)** | ✅ PASS | Attd_08: 08-05 `지각`, **지각 1분** — 1분 차이로 판정이 정확히 갈림 | 〃 |
| **D-1 거부 ★★** | ✅ PASS | 08-07 2차 구간 OT `15:00~19:00` 저장 → **`초과근무 시간이 스케줄 외 허용 범위를 벗어났습니다`**(`ATTD_400_012`). 교정 전이면 240분 전량 허용되던 형상 | `D1-ot-deny-result.png` |
| **D-1 허용 ★★** | ✅ PASS | OT `18:00~19:00` 저장 성공, DB `WORK_MINUTES=60` — **"240→60" 교정의 실화면 재현** | `D1-ot-allow-result.png` + DB |
| **E-1 (반차+무출근)** | ✅ PASS | Attd_11: QTUSERA `미출근 1`(08-04). 지각 1·1분/조퇴 0 도 Attd_08 과 일치 | `P6-attd11.png` |
| (회귀) 구데이터 | ✅ | 07-17 지각 60분 등 기존 판정 변화 없음. 08-07 조퇴 0(작업 전엔 14:00 퇴근=조퇴 오판 형상) | 〃 |

★ **FE 칩 불일치 실측 (잔결함 후보 → §사용자 확정 필요)**: 일자상세 OT 블록의 `등록 가능` 칩이 `15:00 ~ 19:00` **전체**를 표시(FE 자체 계산 = 실근태−스케줄, **연차 면제 미반영**). 서버는 15:00~18:00 을 거부하므로 **칩과 저장 결과가 불일치** — 관리자가 칩을 믿고 입력하면 거부를 결함으로 오인할 소지.

★ 실행 노트(부수 발견): ① Attd_07 캘린더 셀은 **더블클릭**으로 열림 ② 일자상세 2구간 입력엔 순서 가드("2구간 출근은 1구간 퇴근보다 늦어야") 실동작 ③ 시간 입력은 마스크드(:value+@input)라 기존값 수정은 키입력 필요 ④ 과거일 반차 사후 신청 서버 허용(가드=미래·마감·중복만) 실증.

### 그룹 Q·I·P-1·F·P5·M·L-1 — 04:0x~05:0x 실측

| ID | 결과 | 실측 내용 | 증거 |
|---|---|---|---|
| **Q (반차↔시간차 겹침)** | ✅ PASS | 08-12(시간차 10~11시 확정 존재)에 시작기준 반차(쉼 09~14) 제출 → **`이미 신청한 시간차 연차와 시간이 겹쳐요. 겹치지 않는 시간으로 신청해 주세요.`** 거부 | `Q-overlap-0812.png` |
| **I-1 (사용/사용예정 표기)** | ✅ PASS | MyLeaveSummaryView: `사용 2일 (반차 4회, 시간차 30분)` / `사용예정 1일 (반차 1회, 시간차 1시간)` — **B안 형태, 역환산 소멸**. 검산: 반차 5건=과거4+미래1, 종일 2일(사용)+1일(예정), 시간차 30분/1h 각각 정위치 — **전 항목 분해 정합** | `I-myleave-summary.png` |
| **I-2 (소비처 값 일치)** | ✅ PASS | MyPage 연차 요약 카드 = MyLeaveSummaryView 와 **완전 동일 값**(잔여 10일 2시간 12분 / 예정 1일·반차1·1h / 사용 2일·반차4·30분) | `I-mypage.png` |
| **P-1 (잔여 ⓘ)** | ✅ PASS | 잔여 `10일 2시간 12분` — 검산 10.275일×480=132분 **정확**. ⓘ 시트: "잔여 연차의 시간 표기는 참고용 근사치… 이미 사용한 시간은 정확한 값" + 시트 스크롤 잠금/해제(hidden→visible) | `P1-info-sheet.png` |
| **F (주52 소정 감소)** | ✅ PASS | Attd_15: QTUSERA 08.03~08.09 **스케줄기준 16시간·정상** — 사전 계산(종일연차 0 + 반차일 240×4=960분)과 **정확 일치**. 미반영이면 40시간. 실제기준 14시간 59분(179+180+480+OT60)도 정합 | `F-attd15-week52.png` |
| **P5 (앱 관리자 근태현황)** | ✅ PASS | AdminAttdDetail 일자 근태: 08-05 `14:01~18:00 지각` / 08-06 `14:00~18:00`(배지 없음=정상) / 08-07 `09:00~19:00 2구간`(배지 없음) — **웹면과 동일 판정** | `P5-admin-back*.png` |
| **P6 (대시보드)** | ⚠️ 부분 | 렌더·집계 오류 없음. 단 당일(08-08) 기준 위젯이라 시드일 판정 대조 판별력 없음 — 심층은 백엔드 검증 라운드(합성 술어 28조합)로 갈음 | `P6-dashboard.png` |
| **M (승인 흐름)** | ✅ PASS | 일자상세 요청 카드 `연차 사용 요청 · 반차 · 0일 4시간 차감` → 승인 → REQ `2026080800190` STATUS `02`, 처리자 QTHR | `M-approve-*.png` + DB |
| **L-1 (버튼 규약)** | ✅ PASS | 개인정보 수정: `저장 pe-btn--primary x=16 w=174` / `취소 pe-btn--ghost x=198 w=176` — 왼쪽=진행·primary / 오른쪽=이탈·ghost / 폭 균등 | `L1-profile-buttons.png` |
| **P4 (PUSH enqueue)** | ✅ 대체PASS | outbox 6행 적재(`LEAVE_APPROVAL_TURN`×5 → QTHR, `LEAVE_RESULT_APPROVED`×1 → 신청자). `SEND_STATUS=FAILED` = 로컬 firebase 키 없음(예상 동작). **지각 PUSH 실발화는 앱 셀프 출퇴근 전용 → 실기기 잔여** | DB 실측 |
| (부수) 중복 점유 거부 | ✅ | 08-10(승인 종일 연차 기존재) 반차 제출 → 서버 거부 | DB 대조 |

---

## 정리(cleanup) 대상 누적

전부 **개발 DB(QT 통합테스트사업장 00010)** 한정. 운영 무접점.

| # | 무엇 | 데이터 | 방법 |
|---|---|---|---|
| 1 | QTHR·QTUSERA 약관 동의 행 | terms agree | 정리 불요(무해) |
| 2 | 반차 REQ 6건 | `tb_user_attd_req` 2026080800189(08-11 미결)·190(08-04 **승인됨**) + 08-05/06/07 분 + 08-12 거부건은 미생성 | 존치 무해. 지우려면 앱 연차 삭제 요청 경로(3단계) 또는 QT 시드 리셋 때 일괄 |
| 3 | 반차 USE 5건 | `tb_user_leave_use` LV2026080800101(08-11) 외 08-04~07 각 1건, 총 2.5일 차감 | 〃 (QTUSERA 잔여 12.775→10.275) |
| 4 | 근태 4행 | `tb_user_attd_mgmt` 08-05(1401~1800)·08-06(1400~1800)·08-07(2행) | Attd_07 일자상세 전체삭제로 제거 가능. 존치 무해 |
| 5 | OT 1행 | `tb_user_overtime_mgmt` 2026080800047(08-07 1800~1900, 60분) | 일자상세 초과근무 삭제. 존치 무해 |
| 6 | outbox 6행 | `tb_noti_outbox` FAILED | 존치 무해 |
| 7 | 하네스 파일 | `attd-e2e/on/*.mjs`, `smoke-*.mjs`, `lib/browser.mjs` 게이트 처리 추가 | **존치 권장** — 재검증 시 그대로 재실행 가능 |

---

## 실기기·후속 잔여 (이번 무인 테스트로 대체 불가 또는 미실행)

### 기기 고유 (실기기만 가능)
1. **지각·조퇴 PUSH 실수신** — 발화 자체가 앱 셀프 출퇴근 경로 전용(관리자 입력은 미발화·설계대로). enqueue 는 검증됨
2. QR 카메라·네이티브 권한·웹뷰 렌더 품질
3. iOS/안드로이드 실기기에서 반차 카드 UI (스크린샷은 Chromium 기준)

### 시간·시드 제약으로 미실행 (다음 무인/실기 세션에서 가능)
4. **야간 오버나이트**(A-6·C-6·D-4·C-8) — QTOVN(22:00~04:30) 배정 + 야간 반차. ★부수 발견: QTOVN 시드 자체가 "휴게분 60 ↔ 휴게시각 30분 불일치" 형상이라 그 검증까지 겸할 수 있음
5. **가불 반차**(P-3·회수)
6. **일용직 회귀**(O — QTDAILY1 QR 출퇴근·OT 차단 `ATTD_400_152`)
7. **사업장 이동 차단/통과**(G-4·G-6 — `USER_400_073`)
8. **구 반차(시각 NULL) 무회귀**(R) — QT 에 해당 표본 존재 여부부터 확인 필요
9. 하도급 공유 스냅샷(상대 회사 계정 필요) / 대시보드 심층
10. 2구간 스케줄(QT2SEG)·9h(QT9H) 분모 변형 경계 — A-4·A-5 (경계 13:30 형상)

### 이번 무인 테스트로 실기기에서 생략 가능해진 것
- 경계 산출·저장 규약·판정 4경로 대조·OT 게이트·겹침·표기·주52·잠금·승인 — **화면 조작 + DB 검증까지 끝났으므로 실기기에서는 눈으로 훑는 수준이면 충분**

---

## 재실행 방법 (다음 세션·재검증용)

```
cd C:\PRAFTA\attd-e2e
node smoke-overnight.mjs              # 로그인 스모크 (서버 3개 켠 상태)
node on/p3-submit-halfday.mjs 08 11 START   # 반차 신청 (월 일 파트)
node on/p5-attd07-input.mjs 6 1400 1800     # 관리자 근태 입력 (일 출근 퇴근 [출2 퇴2])
node on/p7-ot-d1.mjs 1500 1900 deny         # OT 저장 시도 (시작 종료 태그)
node on/p6-judge-web.mjs / p8*, p9*, p10*, p11*  # 판정·표기·승인 대조
```
전제: 백엔드 8080 · 웹 8081 · 앱 8082(https) 기동, 계정 QTHR/QTUSERA(`QtTest!2026`).
