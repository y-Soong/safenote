# PRAFTA 서비스 정책서 — 종합 정리본

> 이 문서는 `.claude/context/policies/` 아래 3개 정책서(공통 v1.1 · 근태관리 v1.0 · 요청승인관리 재기획 v0.1)와
> 최신 구현 결정사항을 **하나로 합쳐, 처음 보는 사람도 서비스 전체를 이해할 수 있도록** 풀어 쓴 것입니다.
> 각 절 끝의 **💡 쉬운 설명**은 정책 원문에 없는 보충 해설입니다.
> 충돌 시 우선순위: **요청승인관리 재기획서 > 공통 정책서 > 근태관리 정책서 > 기술 정책서**.

---

## 0. 서비스 한눈에 보기

**PRAFTA**는 건설·제조 현장의 **근태관리(출퇴근·연차·정산)** 와 **산업안전관리(TBM·위험성평가·안전점검·아차사고)** 를 묶은 B2B SaaS다. 고객사(회사)가 계약하면 그 아래에 **사업장 → 조직도(부서 트리) → 사용자**가 들어간다.

| 구분 | 내용 |
| --- | --- |
| 주 사용자 | 관리자(인사·안전 담당), 현장 관리자, 근로자(정규/일용직) |
| 채널 | ① 관리자 웹(Vue) ② 모바일 앱(Flutter 셸 + 내부 webview Vue) |
| 기술 | 백엔드 Spring Boot + MyBatis (MySQL 8.0.42) / 프론트 Vue 3 / 모바일 Flutter |
| 격리 단위 | **사업장**이 최상위 격리 단위 (조직도·사용자·데이터는 사업장 단위로 분리) |
| 두 모듈 | **PRAFTA(근태)** + **SAFETY NOTE(산업안전)** — 공통 기반(계정/조직/권한)을 공유 |

**💡 쉬운 설명** — 모바일은 "껍데기는 Flutter, 알맹이는 Vue 웹"인 하이브리드다. Flutter는 카메라·GPS·QR 같은 네이티브 권한과 화면 띄우기만 담당하고, 실제 업무 로직(연차 계산, 결재)은 전부 웹뷰 안 Vue가 한다. 백엔드 API는 한 벌을 웹·앱이 공용으로 쓴다.

---

## 1. 핵심 개념 사전 (먼저 알아둘 용어)

| 용어 | 한 줄 정의 |
| --- | --- |
| **회사(Company)** | 계약 주체. 최상단. `CMPNY_CD`로 모든 데이터가 묶임 |
| **사업장(Site)** | 시스템 최상위 **격리** 단위. 현장 하나 = 사업장 하나 |
| **조직도/노드(Node)** | 사업장 아래 고객이 자유롭게 만드는 부서 트리(본부/부/팀…). 사용자는 **정확히 1개 노드**에 소속 |
| **담당 정 / 담당 부** | 노드에 지정된 정·부 관리자. 그 노드 소속자의 근태·휴가·안전을 승인 |
| **자체근태승인여부** | 노드 체크박스. ON이면 그 노드가 스스로 승인, OFF면 상위 노드로 승인 위임 |
| **권한(Role)** | 메뉴 × CRUD × 플랫폼(WEB/APP) 조합으로 정의된 화면 접근 단위 |
| **사업장 권한** | 사용자가 접근 가능한 사업장 목록(소속 사업장은 자동, 그 외는 명시 부여) |
| **정규 사용자 / 일일계정** | 정규직(회원가입, 영구) vs 일용직(QR 발급, 1일 수명) |
| **계정 슬롯** | 사업장별로 미리 할당된 "일용직 자리". 요금제로 총량 결정 |
| **점유 유지(hold)** | 슬롯이 자정 만료 배치에서 빠지게 하는 플래그(다일 고용 대응) |
| **선점(처리 잠금)** | 여러 관리자가 같은 건을 동시에 처리하지 못하게 하는 잠금 |
| **스케줄** | 근로자의 **계획** 근무 데이터(일자별 근무유형·구간) |
| **근태기록** | 출퇴근 원본 로그 + 스케줄 + 승인된 변경 + 자동 휴게를 종합한 **정산용** 기록 |
| **출퇴근 원본 로그(원장)** | 근로자가 찍은 출근/퇴근 시각. **삭제·변조 불가** |
| **마감** | 스케줄 마감(계획 확정) → 근태 마감(정산 확정)의 2단계 |

---

# Part A. 공통 기반 정책 (공통 정책서 v1.1)

> 권한·조직·사업장·계정·인증·보안은 **여기서만** 정의한다. 근태/안전 정책서는 이걸 전제로 한다.

## A-1. 계정 및 인증 (공통 §3)

### 회원가입
- **필수**: 회사코드, 아이디(4~10자), 비밀번호(6~15자), 이름, 휴대폰, 사업장, 생년월일
- **선택**: 이메일, 성별
- **약관 동의**: 이용약관·개인정보·위치기반(필수) / 마케팅(선택)
- 정규 회원가입은 슬롯 체계와 **무관**

### 로그인 / 계정 찾기
- 아이디+비밀번호. "아이디 저장" 제공. 권한에 따라 첫 진입 화면 분기.
- 계정 찾기·비밀번호 재설정 = **이름 + 휴대폰 본인인증**

### 세션 관리 (토큰) ⭐
- 로그인 시 **액세스 토큰(1시간)** + **리프레쉬 토큰(48시간)** 발급
- 액세스 만료 후 리프레쉬가 살아있으면 앱 재실행 시 **자동 갱신**, 리프레쉬도 만료면 재로그인
- 로그아웃·세션 종료·브라우저 종료 시 **선점(잠금)은 즉시 해제**

### 계정 상태 (코드 [SYS013])
| 상태 | 코드 | 허용 동작 |
| --- | --- | --- |
| 활성화 | `01` | 전 기능 |
| 잠김/미사용 | `02` | 로그인 차단(관리자 비활성화) |
| 탈퇴 | `03` | 로그인 차단, 이력만 조회 |
| 인증대기 | `04` | 관리자가 생성했으나 휴대폰 인증 미완료 (prafta-036 신설) |

**💡 쉬운 설명** — 휴대폰·이메일·생년월일은 DB에 평문으로 두지 않고 **AES-GCM 암호화**(`_ENC` 컬럼)하고, 중복·검색용으로 **HMAC**(`_HMAC` 컬럼), 화면 표시용으로 끝 4자리(`_LAST4`)만 따로 저장한다.

## A-2. 사용자 트랙 — 정규 vs 일일계정 (공통 §4)

| 트랙 | 대상 | 등록 | 수명 |
| --- | --- | --- | --- |
| 정규 사용자 | 정규직·상시 | 회원가입 | 영구(탈퇴 시까지) |
| 일일계정 | 비정규직·일용직 | QR 발급(슬롯 점유) | 유효기간 내(만료 시 삭제) |

## A-3. 일일계정 & 슬롯 관리 (공통 §5) — 집행 화면 "일일계정관리"

현장마다 매일 바뀌는 일용직을, 일일이 가입/탈퇴시키는 대신 **사업장별로 미리 잡아둔 N개의 슬롯(자리)** 에 일일계정이 들어갔다 나갔다 하게 운영한다.

- **슬롯 총량 = 요금제 기반**. 마스터관리자가 사업장별로 배분. 화면에서도 조정 가능
  (상한=잔여 요금제 한도, 하한=현재 점유 수). 변경은 **감사 로그** 대상.
- **기본 수명 1일** = 발급 당일 자정까지. **자정 배치**가 점유 유지 OFF 슬롯을 일괄 만료(계정 삭제 + 빈 자리 복귀).
- **점유 유지(hold) ON** = 자정 만료 제외 → 다일 고용 대응. OFF로 되돌리면 다음 자정에 만료.
- 발급 경로 2가지:
  | 경로(slotKind) | 매핑 | 방식 |
  | --- | --- | --- |
  | 직접가입 `self` | 자동(첫 빈 슬롯) | 근로자가 회원가입 QR/코드로 직접 가입 |
  | 관리자 QR `qr` | 수동(관리자가 슬롯 지정) | 관리자가 빈 슬롯+이름 입력 → 1회용 QR → 근로자 스캔 |
- **차단**: 슬롯 전량 점유 / "계정 등록" 토글 OFF / 사업장 종료일 경과(점유 중 계정도 자동 삭제)
- 슬롯점유 이력 조회: 최대 1개월(−30일), 이름·연락처 **마스킹**(김○○, 010-****-1234)

**💡 쉬운 설명** — 슬롯은 "호텔 객실"이라 생각하면 쉽다. 객실 수(슬롯 총량)는 요금제로 정해지고, 손님(일용직)이 체크인(점유)·체크아웃(만료)한다. "점유 유지"는 장기 투숙 표시다.

## A-4. 사업장 관리 (공통 §6)

| 항목 | 필수 | 설명 |
| --- | --- | --- |
| 사업장번호/명 | 필수 | 고유 코드, 표시명 |
| 주소/상세주소 | 필수 | **지오펜스 중심 좌표** 산출 기준 (주소 → 카카오 지오코딩 → LAT/LON) |
| GPS 허용범위 | 선택 | 지오펜스 반경(m). **0 또는 미설정 시 GPS 판정 미사용** |
| 사업개시일/종료일 | 선택 | 종료일 경과 시 자동 비활성화 |

**💡 쉬운 설명** — 사업장마다 GPS 반경을 따로 준다(예: 200m, 500m). 출퇴근 시 이 원 안/밖으로 "근무지 외" 여부를 판정한다. 반경을 0으로 두면 그 사업장은 GPS를 아예 안 본다.

## A-5. 조직 관리 (공통 §7)

- 사업장마다 **독립 조직도**. 최상위 노드 = 사업장 자신. 하위는 고객이 자유 정의.
- 모든 사용자는 **1인 1소속**(정확히 한 노드).
- **담당 정(필수)/담당 부(선택)** 지정 → 그 사람의 소속이 해당 노드로 자동 변경. 그 노드 소속자 관리 권한 부여.
- 동일 노드 복수 관리자 = **병렬 처리(누구든 1명 처리 시 종결)**, 동시 처리는 **선점**으로 방지.
- **자체근태승인여부**: ON = 그 노드가 직접 승인 / OFF = 상위 노드로 위임.
- 타사업장 조직 가져오기(구조만 복제, 담당자 지정은 미복제). 소속자 있는 노드는 삭제 불가.

## A-6. 권한 체계 (공통 §8) ⭐ PRAFTA 핵심

최종 접근 권한 = **3축 AND**(하나라도 막히면 불가).

```
[화면 권한 Role]  AND  [사업장 권한]  AND  [조직 스코프]
   메뉴×CRUD×플랫폼      접근가능 사업장      소속 vs 타사업장
```

### ① 화면 권한(Role) — 시드 3종
마스터관리자 / HR / 산업안전관리자. 마스터관리자는 자유 생성·수정 가능(단 마스터관리자 권한 자체는 삭제·비활성화 불가).
각 메뉴별 속성: 사용여부·메뉴사용처(WEB/APP/둘다)·조회·생성·삭제·저장·엑셀.

### ② 사업장 권한 + 사이트 스위처
소속 사업장은 자동 접근, 그 외는 명시 부여. 복수 사업장 권한자는 모바일 상단에서 **현장 전환**(전환 시 컨텍스트 완전 격리).

### ③ 조직 스코프 — **소속 사업장 vs 타 사업장에서 규칙이 다르다**
| | 조회 범위 | 관리 권한 |
| --- | --- | --- |
| **소속 사업장 내** | 자기 부서 노드 + 그 하위(자손) | 그 노드의 담당 정/부일 때만 |
| **타 사업장**(권한 부여받음) | 해당 사업장 **전 부서** | 화면 권한(CRUD)만으로 판단 |

**마스터관리자 예외**: 조직 스코프 무시, 전 사업장·전 부서·전 화면(단 사업장 권한은 기본 전체 자동 매핑).

**💡 쉬운 설명** — "내 회사(소속)에서는 내 부서랑 아랫부서만, 대신 내가 부서장이어야 결재한다. 남의 현장(타 사업장)에 파견 권한을 받으면 그 현장 전체가 보이고, 화면 권한만 있으면 결재한다." 본사 → 지사 감독 구조를 자연스럽게 표현한 설계다.

## A-7. 선점(처리 잠금) (공통 §9)

- 관리자가 처리 건을 **열면 선점** → 다른 관리자는 읽기만. 완료/로그아웃/세션종료 시 **즉시 해제**. **타임아웃·강제해제 없음**.
- 적용: 근태 승인·보정·연차·안전 시정조치 등 복수 관리자 관여 승인 화면 전부.
- 제외: **마감/해제 같은 단독 실행 액션**(별도 권한으로 통제).

## A-8. 알림/공지 (공통 §10)
- 채널: 푸시(앱) / 인앱(배지·토스트) / 이메일(공지·마감)
- 트리거: 요청 등록 → 승인권자 / 처리 → 요청자(사유 포함) / 예정일 도래 → 리마인더
- 오발송 방지: 조직 스코프 위반 시 발송 차단 + 중복 발송 방지

## A-9. 보안·개인정보·위치정보 (공통 §11)
- 원칙: 목적 제한·최소 수집·최소 권한·이력 보존
- 위치: **출퇴근 등록 시점에만** 최소 처리(상시 추적 금지). 상세 위치 조회는 별도 권한.
- **감사 로그 대상**: 권한/사업장권한 변경, 조직 변경, 사용자 상태 변경, 다운로드·상세위치 조회, 중요 삭제, 일일계정관리 액션(점유유지·슬롯비우기·최대슬롯·계정등록 토글)

## A-10. 약관 (공통 §12)
이용약관/개인정보/위치기반(필수)·마케팅(선택). **버전 관리**(개정 시 신규 버전, 이력 보존). 필수 약관 개정 시 **재동의 요청**. 동의 이력은 사용자×약관×버전별 시각 보존.

## A-11. 공통 UI/UX (공통 §13)
- 컬러: 중립 베이스 + Primary Green `#16A34A`, Danger `#EF4444`. 메인 White, Header/LNB는 Primary Tint.
- 모바일: 최대 390px 폭, 최소 44px 터치.
- **되돌릴 수 없는 액션(승인/반려/삭제)은 항상 커스텀 확인 모달**(네이티브 confirm 금지, `role="dialog"`).
- 차단 상황(본인결재·마감·선점·권한없음·사업장 미조회/종료)은 배지·배너로 명확히. 처리 이력은 타임라인.

---

# Part B. 근태관리 정책 (근태관리 정책서 v1.0)

## B-0. 근태 핵심 운영 원칙 (근태 §1)
- **원장(출퇴근 원본 로그)은 삭제·변조하지 않는다.**
- 스케줄은 계획, 정산값은 규칙으로 산출(정규/추가근무 산정 + 자동 휴게).
- 변경은 **요청 → 승인/반려 → 반영**, 모든 이력 보존.
- 마감 후 수정은 **마감 해제 → 수정/승인 → 재마감**으로만.
- **1일 최대 2구간 근무 / 1일 최대 2회 출근.**
- 휴게시간은 **자동 공제만**(수동 입력·예외 승인 없음).
- 휴가 승인일·시간휴가 구간은 **노무 수령 거부**(출근 차단).
- GPS 미확인은 등록은 허용하되 "확인 필요"로 관리(마감 전 해소).

## B-1. 근무 유형 / 교대 (근태 §3, §4)
- **근무 유형**(근무 타입 관리 화면): 근무코드, 근무구간(1/2구간), 구간별 시작/종료·휴게, 사용여부, 기본근무여부.
  - 자정 경계 근무 허용(예: 15:00~24:00, 00:00~07:00). 2구간은 **겹침 금지**(구간1 종료 ≤ 구간2 시작).
- **교대 스케줄**: 여러 근무 유형을 반복 패턴(사이클 N일)으로 조합. 적용 시 스케줄 자동 생성. 마감 기간 제외 미래만 반영.

## B-2. 출퇴근 횟수·구간 제한 (근태 §5)
| 스케줄 | 최대 출근 | 비고 |
| --- | --- | --- |
| 1구간 | 1회 | 출근→퇴근 |
| 2구간 | 2회 | 출근→퇴근→재출근→퇴근 |
| 스케줄 없음 | 제한 없음 | **단, 전량 추가근무 승인 필수** |

- **재출근은 반드시 이전 퇴근 완료 후**에만(미퇴근 재출근 시도 차단 → 근태 보정으로 해소).
- 구간 수 초과 출근은 시스템 차단 + "초과근무 상신으로 처리하세요" 안내.

## B-3. 스케줄 관리 (근태 §6)
- 근로자별 **기본 스케줄**(예: 09:00~18:00) → **연간 일괄 자동 생성**.
- 기본 스케줄 변경 시 마감 제외 미래에 일괄 반영. **미래 휴가는 항상 유지(고정)**, 수동 수정 스케줄은 덮어쓰기/유지 옵션.
- 스케줄관리 화면: 사용자별 월 캘린더, 필터(사업장/부서/월/이름), 근무·연차 일괄 적용, 엑셀 업/다운.
- **수정 경합 방지**: 근로자 수정 요청이 대기 중인 일자는 관리자가 직접 수정 불가(먼저 승인/반려).

## B-4. 출퇴근 정책 + GPS (근태 §7) ⭐
- 모바일에서 출근/퇴근 등록. 2구간일은 최대 2회가 정상.
- **GPS 지오펜스 판정**: 사업장 허용범위 기준 내부=정상 / 외부="근무지 외" 태그(정산값 자동 변경 없음).
- **GPS 미확인**(수신 불가·권한 거부·오프라인): 등록은 허용하되 "확인 필요" 상태 → **근태 마감 차단 사유** → 근태 보정으로 해소.
- 오류/누락/중복(출근만·퇴근만·순서 이상·3회 이상): 근태 보정 요청으로 확정(**원본 로그 삭제 안 함**, 보정 이력만 추가).
- **스케줄 없는 날 근무**: 등록 허용 + **전량 추가근무 상신·승인 필수**(미승인 시 마감 차단).

**💡 쉬운 설명(앱 구현 보충)** — 야간 2구간 근무는 시간대 분기 처리가 있고(즉시/차단/확인 후 다음 구간), 지오펜스 **밖**에서 출퇴근하면 그게 곧 **외근**으로 판정되어 사유 입력이 필수다(GPS 행 존재 = 외근 판정). 위치권한은 하드 게이트(거부 시 진행 불가).

## B-5. 휴가 / 연차 정책 (근태 §8) ⭐⭐ — 가장 복잡한 영역

연차는 **두 개의 분리된 도메인**으로 운영된다. 헷갈리면 안 된다.

| 영역 | 무엇 | 테이블 | 예 |
| --- | --- | --- | --- |
| **(1) 연차 타입 관리** (PRAFTA-017) | 사용자 신청형/관리자 부여형 **휴가 종류 정의** | `tb_leave_type_mgmt` | 병가·출산·교육·약정·포상휴가 |
| **(2) 법정 연차 정책** (PRAFTA-018) | 회사 **단일** 법정 연차 **부여 규칙** | `tb_leave_policy`(7 axis) | 입사일/회계연도 기준 자동 부여 |

두 영역은 `tb_user_leave_grant.GRANT_TYPE` prefix로 구분: `STATUTORY_*`(법정·시스템 자동) vs `MANUAL_*`(약정·관리자 수동).

### B-5-1. 연차 타입 관리 (§8.1)
속성: 연차코드/명, 연차타입(사용자 신청/관리자 부여), 유급/무급, 법정/약정, 기본일수, **사용단위(일/반일/시간)**, 사용가능기간, 결재여부·단계, 인사팀승인, 증빙여부, 자동부여 기준일.
- 자동부여 기준일 [SYS027]: `01` 입사일 / `02` 생일 / `03` 부여일지정(매년 MMDD)
- 기준일 01·02면 "N개월 전 1일" 오프셋, 03이면 매년 지정일. (02/29 입력 허용, 평년엔 02/28 폴백)

### B-5-2. 법정 연차 7개 Axis (§8.5) ⭐
회사당 활성 정책 **1개**(`USE_YN='Y'`). 일배치로 자동 부여.

| # | Axis | 옵션 |
| --- | --- | --- |
| 1 | 부여 기준 | `HIRE_DATE` 입사일 / `FISCAL_YEAR` 회계연도 |
| 2 | 회계연도 시작 월/일 | (회계연도 기준일 때만) |
| 3 | 첫해 방식 | `MONTHLY_ONLY` 월차만 / `PRORATE` 비례 / `NEXT_YEAR_BULK` 차년도 일괄 |
| 4 | 비례 반올림 | 올림/반올림/내림/0.5일절사 (PRORATE일 때만) |
| 5 | 근속 가산 | LEGAL(n=3,m=2,max≥25) / CUSTOM |
| 6 | 유효기간 | **12개월 고정**(법정) |
| 7 | 사용촉진 | Y/N |

**Cross-axis 규칙(중요)**:
- `HIRE_DATE` → 첫해는 `MONTHLY_ONLY`만 허용
- `FISCAL_YEAR` → 첫해는 `PRORATE` / `NEXT_YEAR_BULK`만(MONTHLY_ONLY 차단)
- 1년 미만 **월차**(근로기준법 §60②: 매월 만근 시 1일, 최대 11일)는 **axis와 무관하게 항상** 발생.
- 1년 미만 월차 소멸: 발생일별이 아니라 **"입사일+1년−1일"에 일괄 소멸**(§60⑦).

### B-5-3. 멱등성 + 기부여 보호 (§8.5.8) — 절대 규칙
- 모든 자동 부여는 `IDEMPOTENCY_KEY`로 **중복 차단**(예: `{USER}_{YYYY}_ANNUAL`, `{USER}_{YYYYMM}_MONTHLY`).
- **이미 발생한 연차는 사후 차감하지 않는다.** 취소는 `STATUS='CANCELED'` 소프트 처리(데이터 보존).
- STATUS [SYS040] 4종: `ACTIVE`/`EXHAUSTED`/`EXPIRED`/`CANCELED`.
- **`tb_user_leave_use`(사용 이력)는 어떤 경우에도 삭제 안 함.**

### B-5-4. 입사일 변경 처리 (§8.5.6 + prafta-030/032)
입사일이 바뀌면 이미 준 연차를 어떻게 할지가 문제. 처리 방식:
- `KEEP_AND_BACKFILL` — 기존 유지 + **부족분 차액만 1건 추가**(절대 안 줄임).
- `KEEP_AND_APPLY_NEW` — 기존 유지 + 신규 부여만 새 기준(소급 없음).
- `RESET_ALL` — 기존 `STATUTORY_*`를 CANCELED 처리 + 전체 재발급(사용 이력은 보존).
- 모든 변경은 `TB_USER_HIRE_DATE_HISTORY`에 영향 스냅샷 포함 영구 보관, 사유 필수.

> ⚠️ **최신 결정(prafta-032)**: SYS039 "처리방식 자동선택"은 폐기되고, **관리자가 목표 부여량을 직접 입력**하는 수동 조정 방식으로 전환됨. 회수 가능량 = ACTIVE 법정 SUM(부여−사용).

### B-5-5. 사용 단위 정책 (§8.5.9, `tb_leave_usage_policy`)
회사 정책과 1:1. 토글: 1일(항상 Y)/반차/시간차 2h/1h/30분. 휴게시간 가로지르는 시간대 선택은 화면 차단, 1일 환산 시간은 스케줄 비례 자동 계산.
> 최신(prafta-024): `ALLOW_*` 토글은 단일 `USAGE_UNIT`으로 통합 전환 중, `MAX_DAILY_REQUEST` DROP.

### B-5-6. 출근 차단 — 노무 수령 거부 (§8.3)
- **일 단위 휴가 승인일**: 출근 등록 자체 차단(얼럿).
- **시간 단위 휴가 구간**: 그 구간 내 출근·재출근 차단.

## B-6. 근태기록 산정 (근태 §10) ⭐
### 정규 근무 계산 (스케줄 09:00~18:00 기준)
- 일찍 출근해도 정규 시작은 09:00, 늦게 퇴근해도 정규 종료는 18:00.
- 늦으면 **지각**(09:03→3분), 일찍 가면 **조퇴**(17:45→15분).

### 출퇴근 시간 표준화 (§10.2)
- 시작/종료 표준화 단위를 각각 독립 설정(설정안함/5/10/15/30분).
- 방향: **출근 올림 / 퇴근 내림**(회사 유리), 규칙 위반 시 미적용("조건 미충족" 표시).
- 실제 시각은 유지, 계산에만 반영. 변경 이력 자동 기록.

### 추가근무(초과근무) 계산 (§10.3)
- 계산식: **(초과분 − 면제시간)을 추가근무 표준화 단위로 산정**.
- 예(출퇴근 30분·추가근무 15분 단위): 18:59 퇴근 → 59−30=29 → **15분 인정**, 19:00 → 60−30=30 → **30분**.

### 자동 휴게 공제 (§10.4)
정규 근무시간 기준(추가근무 제외), 근무 유형별 설정값 자동 공제(수동 불가).

## B-7. 근태 보정 (근태 §11)
- 대상: 출퇴근 누락/중복/순서/3회 이상, 시간휴가 구간 오류, 일자 귀속(교대·자정 경계), **GPS 미확인 해소** 등.
- 신청(모바일): **과거·현재만**(미래 차단), 오늘 근무중이면 비활성, 마감 후 불가.
- 요청-승인, 사유·이력 필수, 선점 적용. **원본 로그 read-only, 보정 결과만 추가.**

## B-8. 마감 (근태 §12, §13) ⭐
**순서: 스케줄 마감 → 근태 마감**(스케줄 마감 선행 필수).
- 마감 단위: **조직(노드) + 월**. 마감 권한자만 실행(선점 미적용, 별도 권한).
- 마감 후 변경 = 마감 해제 → 수정/승인 → 재마감.
- **근태 마감 차단 조건**:
  | 차단 사유 | 해소 |
  | --- | --- |
  | 미결(대기) 요청 | 승인 또는 반려 |
  | GPS 미확인 미해소 | 근태 보정 승인 |
  | 미승인 추가근무 | 추가근무 승인/반려(스케줄 없는 날 포함) |

> 최신(prafta-028): 마감 단위가 **부서 단위(NODE_CD/'*' + 하위 cascade)** 로 확장. 권한 master/hr/노드관리자. 마감 시 쓰기 차단(BE+FE 3화면).

## B-9. 근태 현황 조회 (근태 §14)
마감 전 정산 프리뷰·실시간 현황. 필터(사업장/조직/기간), 정규/비정규 분리, 상태 집계(근무중/미출근/휴무/퇴근), 사용자 드릴다운, 엑셀.
> 보충(prafta-034): 월별 근태판정 화면(Attd_11) — 근무일수/시간/초과/지각·조퇴 월집계(읽기 전용).

---

# Part C. 요청 승인 관리 (요청승인관리 재기획서 v0.1) ⭐ 단일 출처

> 근태 §9.6의 "요청 승인 관리 화면"을 별도로 재기획한 **단일 출처**. 화면 구성·처리 흐름은 이 문서가 이긴다.

## C-1. 핵심 변경 사항 (재기획 §3)
1. **3탭 → 4탭**: 스케줄 수정 / **근태 보정**(신규 통합) / 초과근무 상신 / 연차 상신
2. **초과근무 사후 상신 기한: D+5일 → "해당 일자 근태 마감 전까지"** (D+5 폐기)
3. **자동 마감 금지**, 강제 마감 미도입(처리 필요 건 남으면 마감 차단)
4. v1 스코프: **임금 정산 미포함**(승인 효과는 "근태기록 결정 마킹"까지)
5. 용어: "결재" 미사용 → 공식 표현은 **승인/반려, 신청/상신**

## C-2. 화면 구조 (재기획 §5)
2분할: 좌측 접수함(리스트) 60% + 우측 상세 패널 40%.
- **4탭** + 각 탭 대기 건수 배지
- 필터: 조직·사용자명·상태(대기/승인/반려/전체, 기본 대기)·기간(기본 30일)·정렬(요청일시/마감임박순)
- 접수함 컬럼: 체크박스·요청자·요청일시·대상일자·요약·상태·마감(D-N영업일, D-1↓ Danger, D-3↓ Amber)
- 일괄 처리: 같은 탭·모두 대기·차단행 미포함일 때만. **일괄 승인은 "요청대로 승인"만**(시간 조정 불가).
- 상세 패널 6섹션: ①메타 ②검증·제약 ③Before/After 비교 ④사유·증빙 ⑤관리자 결정(승인/조정후승인/반려) ⑥처리 이력 타임라인

## C-3. 4종 결재 플로우 (재기획 §6)

| 탭 | 정책 | 신청 조건 | 관리자 처리 | 승인 효과 |
| --- | --- | --- | --- | --- |
| **스케줄 수정** | §9.2 | 스케줄 존재 + 스케줄 마감 전(**시간 방향 제한 없음**) | 승인/조정후승인(시간·코드)/반려 | 스케줄 갱신 + 정산 재계산 |
| **근태 보정** | §11 | 근태 마감 전(과거·현재만) | 승인/조정후승인(보정 시각·구간)/반려 | 근태기록 보정(원본 보존), GPS 미확인 해소 |
| **초과근무 상신** | §9.3 | **사후(과거·현재)만**, 출근 기록 1건↑ 존재, 마감 전 | 승인/조정후승인(시간)/반려, 3단 비교(계산값/상신값/승인값) | 추가근무 정산 반영 |
| **연차 상신** | §9.4 | **출근 기록 없을 것** + 마감 전(시간 방향 무관). 스케줄 없는 날은 **full-day만** | 승인/반려(**시간 조정 없음**), 다단/HR최종 반영 | 휴가 적용 + 출근 차단 |

**💡 최신 게이팅 변경(2026-05-31, prafta-app-013)** — 모바일 신청 게이팅이 정책서를 덮어쓰며 바뀜:
- 스케줄 수정: "미래만" 제한 폐기 → 스케줄 존재 + 미마감이면 과거·현재·미래 모두.
- 초과근무: "사전/사후 모두" → **사후(과거·현재)만, 미래 차단**. 조건도 "전구간 완료"→"출근 기록 존재"로 완화.
- 연차: "스케줄 필수" 폐기 → 출근 기록 없으면 신청 가능, 시간 방향 무관, 스케줄 없는 날은 full-day만.

## C-4. 인터랙션·예외 (재기획 §7, §8)
- **선점**: 상세 패널 진입 즉시 선점, 타 관리자는 "처리중-{이름}" read-only.
- **본인 결재**: 자체근태승인 ON 노드의 본인 건은 자동 승인, 상위 공동 처리 범위에선 본인 건 자동 승인 금지("본인 결재 차단" 표시).
- **마감 차단**: 마감 후 요청은 ② 차단 + 안내(초과근무는 "근태 보정으로 예외 처리" 링크).
- **충돌 경고**: 스케줄↔연차↔출퇴근 충돌 시 Amber 경고.
- **일괄 승인 중 1건 실패**: 전체 롤백 아님, 행 단위 분리(N 성공/M 실패).
- v1에서 요청 후 근로자 취소 불가(반려·재신청으로 처리).

## C-5. 데이터 구조 (재기획 §9)
4종 요청을 **단일 엔티티 `ApprovalRequest` + 서브타입 페이로드**로 모델링(향후 외출/출장 등 확장은 페이로드 추가만).
- 공통 필드: id(REQ-…, 유형접두 SC/AT/OT/AL)·type·status·requester(스냅샷)·site/orgNode·targetDate·payload(JSON)·reason·attachments·lock·decision·history·deadlineAt
- `deadlineAt` = 사업장 attendanceCloseRule을 휴일 마스터로 영업일 변환.

---

# 부록

## 부록 1. 정책서 우선순위 (충돌 시)
1. **요청승인관리 재기획서** (요청 승인 화면·사후 상신 기한·탭 구성의 단일 출처)
2. **공통 정책서** (권한·조직·사업장·계정·인증·보안)
3. **근태관리 정책서** (근태 도메인 규칙)
4. **기술 정책서** (CLAUDE.md/developer.md 등 — 비즈니스 규칙과 충돌 시 비즈니스가 우선)

## 부록 2. 자주 나오는 코드값 (SYS 코드)
| 코드 | 의미 | 값 |
| --- | --- | --- |
| SYS013 | 계정상태 | 01활성 02잠김 03탈퇴 04인증대기 |
| SYS024 | 휴가성격 | 01법정 02특별/약정 |
| SYS025 | 사용단위 | 00일 01반차 02시간2h 03시간1h 04시간30분 |
| SYS027 | 자동부여 기준일 | 01입사일 02생일 03부여일지정 |
| SYS035 | GRANT_TYPE | STATUTORY_* 법정 / MANUAL_* 약정·포상 |
| SYS036~038 | 연차 axis | 부여기준/첫해방식/비례반올림 |
| SYS040 | 연차 STATUS | ACTIVE/EXHAUSTED/EXPIRED/CANCELED |
| SYS041 | 고용형태 | REGULAR/CONTRACT/DAILY/EXECUTIVE |

## 부록 3. 산업안전(SAFETY NOTE) 모듈 — 도메인 요약
근태와 공통 기반을 공유하는 두 번째 모듈. 관련 테이블:
- **TBM(작업 전 안전미팅)**: `tb_tbm_session`(세션), `tb_tbm_attendance`(출결), `tb_tbm_session_content/risk`(콘텐츠·위험성평가 매핑), `tb_tbm_session_state`(실시간 동기화). 모바일에서 입실/종료(서명), 비번 5회 실패 60초 잠금.
- **위험성평가**: `tb_risk_assessment`, `tb_risk_site_hazard`, `tb_risk_type`.
- **안전점검**: `tb_chkpt_type_mgmt`(체크포인트 유형), `tb_chkpt_inspect_item/answer`. QR→점검→저장 흐름.
- **아차사고/사건**(prafta-040, 신규 도메인): `tb_near_miss`(위험성평가와 분리). 신규 대메뉴 "사건관리".

## 부록 4. 알아두면 좋은 구현 사실 (메모리 기반)
- **PII 암호화**: 휴대폰/이메일/생년월일 = AES-GCM(`_ENC`) + HMAC(`_HMAC`) + LAST4. 평문 저장 금지.
- **앱 토큰 오류 코드**: 인터셉터가 `COMMON_400_003`/`600`을 토큰오류로 보고 강제 로그아웃 → 일반 검증 실패엔 `COMMON_400_001` 사용.
- **Lombok boolean 함정**: `boolean isXxx`는 JSON에서 `is`가 탈락(isTwoSlot→twoSlot). `@JsonProperty`로 고정 필요.
- **연차 잔여 계산**: 잔여 = 부여 − SUM(USED_DAYS) (이중 차감 주의). usageRate = usedTotal/granted.
- **DB 컬럼 COMMENT 규칙**: 코드성 컬럼은 `설명[SYS코드] 코드값:의미` 형식 필수.

---

*문서 생성: 2026-06-01 · 출처: `.claude/context/policies/` (공통 v1.1 / 근태 v1.0 / 요청승인 v0.1) + 구현 메모리. 정책 원문이 갱신되면 이 정리본도 다시 뽑아야 한다.*
