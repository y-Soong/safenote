# 코퍼스 파이프라인 — §7 미결정 4건 확정 기록

> 확정일 2026-07-02 · 근거: 작업지시서 `.claude/refs/작업지시서_AI코퍼스_수집적재_파이프라인.md` §7
> "판정은 사람, 실행은 코드" 원칙에 따라 사람이 확정. 파이프라인 구현 시 아래를 그대로 반영한다.

## ① HAZARD_TEXT 매핑 → **전용 필드 있을 때만 매핑, 없으면 NULL**

- 자유텍스트(사고경위)에서 규칙/LLM으로 유해요인을 **도출하지 않는다**(오분류·할루시네이션 위험 회피).
- registry `preprocess.hazard_field` 에 실제 컬럼이 지정된 자료만 그 값을 HAZARD_TEXT 로 매핑.
  - 예: 건설사고사례(15108262) → `hazard_field: null` → HAZARD_TEXT = NULL
  - 예: SIF(15140383) 등 유해·위험요인 컬럼 보유 자료 → 해당 컬럼 매핑
- 검색은 CONTENT 임베딩으로 도므로 NULL이어도 검색 품질에 영향 없음. HAZARD_TEXT는 보조·필터용.

## ② CHUNK_ID 생성 → **content_hash 기반 결정적 ID (멱등 UPSERT)**

- TB_CMM_SEQ(MySQL 순번) 미사용 — 순번은 재실행 시 중복 적재를 유발하고, 별도 PostgreSQL+Python 배치에 부적합.
- 결정적 ID = 동일 입력이면 항상 동일 ID → UPSERT로 멱등 보장(§6). 소스 버전 갱신 시 바뀐 행만 신규 반영.
- **스킴(VARCHAR(40) 준수)**: `{SOURCE_ID}_{sha256(SOURCE_ID + '|' + locator + '|' + CONTENT)[:16]}`
  - 예: `15108262_a1b2c3d4e5f60718` (데이터셋ID 8 + '_' + 해시 16 = 25자, 40자 이내)
  - locator 없으면 빈 문자열. 동일 SOURCE 내 완전 동일 CONTENT는 같은 ID로 자연 dedup(허용).
- 소스 버전 갱신 대응: 파일 날짜 접미사 변경 감지 시 해당 SOURCE_ID 구청크 USE_YN='N' 후 재적재(이력 유지, §6).

## ③ 마스킹 강도 → **익명 치환 + 주소 시군구까지 보존**

- 업체명 → `○○건설`, 성명 → `○○○` 형태 **익명 치환**(문장 구조 보존 → 임베딩 맥락 유지).
- 주소 → **시군구까지만 보존**, 상세(동/번지 이하) 제거. (시군구는 개인 식별정보 아님, 지역 위험패턴 검색에 유용)
- **적용 범위**: `mask_fields` 지정 필드뿐 아니라 **CONTENT(사고경위) 자유텍스트 내 박힌 업체명/성명도 정규식으로 스캔**해 치환.
  - 패턴 예: `(주)X`, `X건설`, `X산업`, `X종합건설`, `X씨`, 성명+직함 등.
- **불확실 시**: 패턴이 애매해 오치환 위험이 크면 해당 필드/구절 **통째 제거 후 로그**(§4-2). 미마스킹 데이터는 어떤 산출물에도 남기지 않음(하드가드 #2).
- ⚠️ **선행 확인 필요**: 실제 raw CSV를 열어 국토안전관리원 데이터가 **원천에서 이미 익명화**돼 있는지 확인 → 마스킹 규칙 최종 튜닝. (이미 익명화면 정규식은 안전망 역할)

## ④ AI유형 자료 3조건(문체부) → **MVP 제외/보류**

- 현재 대상 자료(건설사고사례 "제한없음", SIF)는 AI유형 비해당 → 지금 3조건 이행 설계 안 함.
- 실제 AI유형 라이선스 자료를 registry에 채택하는 시점에 별도 설계:
  1. 동일·유사 산출물 방지 → 기존 **이중 트랙 물리 분리**(verbatim/recompose) 위에 확장
  2. RAG 직접인용 출처명시 → 기존 **tb_ai_corpus_source + SOURCE_LOCATOR** 활용
  3. 학습데이터 재판매 금지 → 계약/정책 사항(코드 아님)

---

## 보류/백로그

- ~~**15140227 (기인물 유해위험요인 PDF) 전처리 보류**~~ → **해결·적재 완료 (2026-07-03)**:
  - data.go.kr 페이지의 'CSV' 표기는 오류였고 실제로는 PDF만 제공(다운로드 버튼에 CSV 없음).
  - Anaconda base가 Python 3.7.3라 base pip(구버전 pytoml)·권한(ProgramData admin) 문제로 설치가 막혔으나,
    **`pip install --user`로 최신 pip + pdfplumber 0.9.0 를 사용자 영역에 설치**(관리자 불필요)하여 해결.
  - `read_pdf`(preprocess.py): pdfplumber `extract_tables()` 괘선 기반 셀 복원 → 개요(page0) 제외,
    순번 숫자 행만, 기인물명 병합셀 forward-fill. 332행 → 332청크(유해요인·대책 100%).
  - ⚠️ 부작용: pdfplumber가 끌어온 cryptography 45가 base pyOpenSSL을 깨 `requests` import 실패 →
    **user-site cryptography 제거**로 해결(암호화 안 된 PDF는 pdfminer가 cryptography 불필요).

## 이 결정들이 남긴 선행 조건 (파이프라인 착수 전)

- [ ] `corpus/raw/` 에 실제 원본 파일 투입 (사용자)
- [ ] `corpus/registry/sources.xlsx` 에 자료별 라이선스 판정 기입 + `license_checked: TRUE` (사람 확정)
- [ ] raw CSV 실물 확인 → ③ 마스킹 정규식 최종 튜닝

---

## 발견 사항 (미수정, 기록만)

### 2026-08-19 · 주소 절단 정규식 오탐 — 본문 단어 소실

`common/masking.py` 의 `_ADDR_DETAIL` 이 **주소가 아닌 일반 문장을 상세주소로 오인**해 뒤 토큰을 절단한다.

- 실측(15053339 시설물사고사례 1번 청크):
  - 원본: `콘크리트 타설시 도면에 대한 재검토 미비`
  - 결과: `콘크리트 타설시에 대한 재검토 미비`  ← **"도면" 소실**
- 원인: 패턴이 `[한글]+(시|군|구)` + 공백 + `[한글]+(동|읍|면|리…)` 를 상세주소로 본다.
  `타설시` → 시군구로, `도면` → 읍면동으로 오인 → "타설시 도면"을 주소로 판단하고 뒤를 잘라냄.
- 위험도: **낮음**. 개인정보가 새는 방향(과소 마스킹)이 아니라 멀쩡한 본문이 깎이는 방향(과잉 마스킹).
  다만 근거 문장의 의미가 미세하게 훼손된다(RAG 근거로 인용될 때 어색해질 수 있음).
- 수정 방향(미적용): 시군구 매칭에 **행정구역 맥락 조건**을 요구한다.
  ① 앞에 시/도 토큰이 있을 때만 매칭(`경기도 안양시 ○○동`), 또는 ② 실제 행정구역명 목록 대조.
- ⚠️ 수정 시 파급: CONTENT 가 바뀌면 `chunk_id`(content 해시) 도 바뀐다 →
  해당 SOURCE 재적재 + 구 청크 `USE_YN='N'` 정리가 함께 필요(결정② 소스 버전 갱신 절차와 동일).
- 판단: 사용자 확인 후 **기록만 하고 수정 보류**(2026-08-19).

### 2026-08-26 · 15121008(국내재해사례 첨부 PDF) — 첨부 문서 템플릿이 연도별로 다름

신규 소스 15121008 착수 시 5건 샘플(2026년 최근 케이스)로 검증한 파서(소형 "OPS" 카드,
x좌표 크롭 좌/우 컬럼 분리)가 748건 규모로는 입력의 91%(680/746)를 제외시키는 문제 발견.

- 원인: KOSHA 첨부 PDF가 최소 3종 템플릿 혼재 — ① 소형 카드(~252×356pt, 최근 케이스) ②
  A4 세로 단일컬럼 서술형(~595×841pt, 과거 다수, 헤더 표기도 "발생원인"/"재해발생원인"/
  "재해발생 원인" 등 문서마다 다름) ③ A4 가로 2단(~841×595pt) ④ 고해상도 스캔 이미지
  (~2352×3969pt, 텍스트 레이어 없음).
- 대응: 페이지 크기로 템플릿 A/B 를 감지해 분기하는 파서로 확장(`preprocess.py`
  `_read_disaster_attach_template_a/b`). ③④는 이번 범위에서 제외(가로형은 컬럼 경계 재실측
  필요, 스캔은 OCR 필요 — 별도 작업).
- 결과: 수율 66→173청크(748건 표본 기준 23%). 상세는
  `.claude/refs/작업지시서_코퍼스4번_15121008_첨부수집_잔여분.md` §4·§6 참조.
- 교훈: **소수 샘플(5건)로 파서를 검증하고 "됐다"고 판단하면 안 된다** — 특히 여러 해에 걸쳐
  누적된 정부 공공데이터는 연도별 양식 변경이 흔하다. 최소 수백 건 규모 표본으로 페이지
  크기/레이아웃 분포부터 먼저 확인한 뒤 파서를 설계하는 게 맞다(이번엔 순서가 거꾸로였음).

### 2026-08-26 · 15069200(화학사고 정보) — mask_fields 회사명이 마스킹 없이 그대로 노출되던 버그

착수 전 검토(assessment) 단계에서 5건 샘플의 `content`/`meta_json`을 열어보니 실제 회사명
(스미세이케미칼/AK켐텍/일진LED/빙그레 등)이 **마스킹 없이 그대로** 들어 있는 걸 발견. 하드가드#2
("마스킹은 필수") 위반이라 적재 보류하고 원인 규명 후 수정.

- 원인 ①: `_COMPANY` 정규식(`masking.py`)이 건설업 접미사(건설/산업/중공업/이엔씨 등) 위주로
  튜닝돼 있어 화학·제조업 회사명(케미칼/켐텍/LED, 브랜드명 등 접미사 없는 이름)을 못 잡는다.
- 원인 ②: known-value 치환(`mask_body`의 `other_values`)이 있긴 한데 **완전일치**만 검사해서,
  CSV 필드값("스미세이케미칼")과 본문 서술("스미세이 케미칼")의 공백 한 칸 차이로도 실패한다.
  방향은 양쪽 다 있었다(필드값에 공백 있고 본문엔 없는 경우도 6/1026건 발견 — "서울과학기술
  대학교" vs "서울과학기술대학교" 등).
- 원인 ③: META_JSON 쪽(`preprocess.py` `transform()`)은 애초에 known-value 치환을 안 쓰고
  `mask_company`/`mask_person` 패턴매칭에만 의존해서, 원인①과 똑같은 이유로 META에도 실명이
  그대로 남았다.
- 수정: `masking.py`에 `_fuzzy_literal()` 추가 — known-value 를 **먼저 despace 한 뒤** 글자
  사이마다 선택적 공백을 끼워 매칭(양방향 공백 차이 다 흡수). `preprocess.py` META 마스킹은
  패턴매칭을 걷어내고, `mask_fields`(non-addr)에 오른 값은 **무조건** `"○○"` 치환으로 변경
  (하드가드#2: "패턴이 애매하면 제거" — 이미 mask_fields 로 식별값임이 확정된 값을 패턴으로
  재확인하는 건 애초에 불필요한 리스크였다).
- 검증: 1,026행 전수 감사(원본 CSV `사고업체명` 값을 despace 후 processed content/meta 전체에서
  재검색) — 수정 전 16건(content)+2건(meta, 오탐 포함해 재확인 필요했던 수치) → **수정 후 0건**.
- 기존 적재분(15053339, 472청크) 영향 확인: `mask_fields=MngMbyNm`인데 실측해보니 **전량 빈 값**
  이라 애초에 새어나갈 실제 값이 없었음 — 재작업 불필요. 15108262(mask_fields=사고명)는 성격상
  PII가 아니라 확인 대상에서 제외.
- 교훈: **known-value 마스킹은 완전일치로 하지 말 것** — 구조화 필드값과 자유서술문의 표기(특히
  공백)는 어긋나는 게 흔하다. despace 후 글자단위 매칭이 최소 방어선. 그리고 **패턴 기반
  마스킹(mask_company 류)에 단독으로 의존하지 말 것** — 이미 mask_fields 로 "이 값은 식별값"
  이라고 스키마 단계에서 확정된 값은 패턴 재확인 없이 무조건 치환하는 게 더 안전하다.

## 2026-08-27~28 — 5번(15144147 KOSHA GUIDE) 임베딩 적재, 밤새 진행 0건의 원인은 중복 프로세스

배치 크기(EMBED_BATCH=16→8, TEI `max_batch_requests=8` 강제에 맞춤)와 타임아웃(120s→180s)을
수정하고 재시도 루프(`/c/tmp/run_embed_loop.sh`)를 재기동했는데도 밤새 pgvector 청크 수가
1,664에서 전혀 늘지 않았다. `docker logs prafta-tei`로 보면 TEI 자체는 계속 임베딩에 성공하고
있었는데(`queue_time` 이 43s→150s 로 계속 늘어나는 추세만 있었음), 커밋(200행 단위)까지 이르는
요청이 하나도 없었다.

- 원인: 디버깅 과정에서 같은 `run_embed_loop.sh`를 백그라운드 Bash 호출로 **여러 번(최소 4~5개)
  중복 실행**해놓고 이전 것들을 정리하지 않았다. 각 인스턴스가 독립적으로 `30_embed_load.py`를
  순차 호출해 TEI에 요청을 쏘고 있었는데, TEI 입장에서는 동시에 4~5개 클라이언트가 경합하는
  꼴이라 큐 대기시간이 계속 불어났다(단일 클라이언트라면 배치당 추론시간 12~19s 수준이 정상).
  중복 인스턴스는 로컬 `python.exe` 프로세스로 남아 EC2 재기동/재접속과 무관하게 계속 살아있었다.
- 진단: `Get-CimInstance Win32_Process -Filter "Name='python.exe'"` 로 로컬에 python 프로세스가
  몇 개 떠 있는지, `ps -ef | grep run_embed_loop`로 중복 wrapper bash가 몇 개인지 먼저 확인했어야
  할 걸 몇 시간 늦게 확인했다. TEI 로그의 `queue_time` 단조 증가는 배치크기 문제가 아니라 **클라이언트
  중복**의 신호였다.
- 수정: 중복 프로세스 전량 종료(로컬 python.exe + bash wrapper) → TEI 컨테이너 재기동(잔여 큐
  비우기) → 딱 하나의 인스턴스만 재실행. 이후 정상 진행(약 1시간 내 1,664→4,664, 스코프 대상
  4,446청크 전량 완료).
- 부수 결함: `run_embed_loop.sh`의 종료 감지(`grep -q "이미 최신"`)가 Windows 콘솔 cp949 출력과
  맞물려 실제로는 매칭에 실패해, 완료 후에도 300회 상한까지 무의미하게 계속 루프를 돌았다(피해는
  없음 — 신규 0건 확인 쿼리만 반복). 다음에 유사 wrapper를 짤 땐 종료 판정을 문자열 grep이 아니라
  종료 코드나 별도 마커 파일로 하는 게 인코딩에 안전하다.
- 교훈: **백그라운드 재시도 스크립트를 다시 돌리기 전엔 반드시 이전 인스턴스가 살아있는지부터
  확인/정리할 것** — "느려진 원인이 코드 버그다"라고 결론 내리기 전에 "혹시 내가 같은 걸 여러 개
  띄워놓은 건 아닌가"부터 봤어야 했다. 특히 여러 턴에 걸쳐 같은 백그라운드 명령을 반복 실행했을
  때 "killed"로 보고된 태스크가 실제 OS 프로세스까지 종료됐다고 가정하지 말 것(이번엔 안 됐었다).
