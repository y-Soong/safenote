-- ============================================================================
-- PRAFTA-019-B — 사용자별 직급 관리
-- 작성일: 2026-05-23
-- 적용 환경: MySQL 8.0.42
-- 참조: .claude/requests/prafta-019-B-user-rank.md, prafta-019-plan.md
--
-- 변경 요약
--  1) tb_user.RANK_CD varchar(10) NULL 추가 (AFTER AUTH_CD)
--  2) BAIM_VAL 직급 코드그룹 신규: COM007 (착수 시 다음 가용 번호 = COM007 확인)
--  3) 기본 직급 7종 시드 (회사 '001'). SORT_IDX = 직급 순서(결재라인 프리셋 근거).
--
-- 멱등성: ALTER ADD COLUMN / INSERT 는 중복 실행 시 에러. 이미 반영된 환경에서는 건너뛸 것.
--         (운영은 회사 '001' 단독. 다회사 확장 시 회사별 시드를 반복할 것)
-- ============================================================================

-- ----------------------------------------------------------------------------
-- 1) tb_user 직급 컬럼 추가
-- ----------------------------------------------------------------------------
ALTER TABLE tb_user
    ADD COLUMN RANK_CD varchar(10) NULL COMMENT '직급 코드 (BAIM_VAL COM007 직급 코드그룹 참조)' AFTER AUTH_CD;

-- ----------------------------------------------------------------------------
-- 2) BAIM_VAL 직급 코드그룹 마스터 (COM007)
-- ----------------------------------------------------------------------------
INSERT INTO tb_baim_val_m (CMPNY_CD, BAIM_VAL_CD, BAIM_VAL_NM, USE_YN, VAL_DESC, INSERT_NO, INSERT_DATE) VALUES
    ('001', 'COM007', '직급', 'Y', '사용자 직급 코드그룹 (prafta-019-B)', 'SYSTEM', NOW());

-- ----------------------------------------------------------------------------
-- 3) 기본 직급 세트 시드 (회사 '001') — SORT_IDX 로 직급 순서 부여
--    상세코드는 기존 BAIM 컨벤션(5자리 zero-pad) 준수.
-- ----------------------------------------------------------------------------
INSERT INTO tb_baim_val_d (CMPNY_CD, BAIM_VAL_CD, BAIM_VAL_D_CD, BAIM_VAL_D_NM, SORT_IDX, USE_YN, INSERT_NO, INSERT_DATE) VALUES
    ('001', 'COM007', '00001', '사원', 1, 'Y', 'SYSTEM', NOW())
  , ('001', 'COM007', '00002', '주임', 2, 'Y', 'SYSTEM', NOW())
  , ('001', 'COM007', '00003', '대리', 3, 'Y', 'SYSTEM', NOW())
  , ('001', 'COM007', '00004', '과장', 4, 'Y', 'SYSTEM', NOW())
  , ('001', 'COM007', '00005', '차장', 5, 'Y', 'SYSTEM', NOW())
  , ('001', 'COM007', '00006', '부장', 6, 'Y', 'SYSTEM', NOW())
  , ('001', 'COM007', '00007', '이사', 7, 'Y', 'SYSTEM', NOW());
