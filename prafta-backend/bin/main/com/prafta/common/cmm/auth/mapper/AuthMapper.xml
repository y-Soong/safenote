<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.prafta.common.cmm.auth.mapper.AuthMapper">

	<select id="selectValidByRefreshTokenHash" parameterType="string" resultType="com.prafta.common.cmm.auth.vo.AuthToken">
	/* AuthMapper.selectValidByRefreshTokenHash */
	    SELECT
			CMPNY_CD            AS cmpnyCd
			, USER_ID             AS userId
			, TOKEN_ID            AS tokenId
			, CLIENT_TYPE         AS clientType
			, DEVICE_ID           AS deviceId
			, REFRESH_TOKEN_HASH  AS refreshTokenHash
			, ISSUED_DTIME        AS issuedDtime
			, EXPIRE_DTIME        AS expireDtime
			, REVOKED_YN          AS revokedYn
			, REVOKED_DTIME       AS revokedDtime
			, IP_ADDR             AS ipAddr
			, USER_AGENT          AS userAgent
			, INSERT_NO           AS insertNo
			, INSERT_DATE         AS insertDate
			, UPDATE_NO           AS updateNo
			, UPDATE_DATE         AS updateDate
	    FROM tb_auth_token
	    WHERE REFRESH_TOKEN_HASH = #{refreshTokenHash}
	      AND REVOKED_YN = 'N'
	      AND EXPIRE_DTIME > NOW()
	    LIMIT 1
	</select>
	
	<select id="selectUserForJwt" parameterType="string" resultType="com.prafta.common.cmm.auth.vo.UserInfo">
	/* AuthMapper.selectUserForJwt */
	SELECT 
		A.CMPNY_CD
		, A.USER_ID
		, A.USER_NM
		, A.USER_PW
		, A.AUTH_CD
        , A.SITE_CD
        , B.SITE_NO
        , B.SITE_NM
		, A.MBL_NO
		, A.EMAIL
		, A.INSERT_NO
		, A.INSERT_DATE
		, A.UPDATE_NO
		, A.UPDATE_DATE
	FROM TB_USER A
         INNER JOIN TB_SITE B
			ON (
				A.CMPNY_CD = B.CMPNY_CD
			AND A.SITE_CD = B.SITE_CD
            )
	WHERE 1=1
	  AND A.USER_ID = #{userId}
	  AND A.USE_YN = 'Y'
	</select>
	
</mapper>