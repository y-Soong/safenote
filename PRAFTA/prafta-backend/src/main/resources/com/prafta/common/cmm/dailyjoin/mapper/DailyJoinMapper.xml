<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.prafta.common.cmm.dailyjoin.mapper.DailyJoinMapper">

    <!-- 회사/사업장 정보 조회 (비로그인 단독 조회) -->
    <select id="selectSiteInfo" parameterType="com.prafta.common.cmm.dailyjoin.application.query.SiteInfoQuery" resultType="com.prafta.common.cmm.dailyjoin.result.SiteInfoResult">
    /* DailyJoinMapper.selectSiteInfo */
    SELECT
        C.CMPNY_CD          AS cmpnyCd
        , C.CMPNY_NM        AS cmpnyNm
        , S.SITE_CD         AS siteCd
        , S.SITE_NM         AS siteNm
    FROM TB_CMPNY C
         INNER JOIN TB_SITE S
            ON (
                C.CMPNY_CD = S.CMPNY_CD
            )
    WHERE C.CMPNY_CD = #{cmpnyCd}
      AND S.SITE_CD = #{siteCd}
      AND IFNULL(C.USE_YN, 'N') = 'Y'
      AND IFNULL(S.USE_YN, 'Y') = 'Y'
    </select>

    <!-- 사용자ID 중복 카운트 (TB_DAILY_USER, USE_YN='Y') -->
    <select id="selectUserIdDupleCnt" parameterType="com.prafta.common.cmm.dailyjoin.application.query.UserIdDupleCheckQuery" resultType="int">
    /* DailyJoinMapper.selectUserIdDupleCnt */
    SELECT COUNT(*)
    FROM (
        SELECT 1
          FROM TB_DAILY_USER
         WHERE CMPNY_CD = #{cmpnyCd}
           AND USER_ID  = #{userId}
           AND USE_YN   = 'Y'
         LIMIT 1
    ) T
    </select>

    <!-- 휴대폰번호 중복 카운트 (TB_DAILY_USER, CMPNY_CD + MBL_NO_HMAC 기준 USE_YN='Y') -->
    <select id="selectMblNoDupleCnt" parameterType="com.prafta.common.cmm.dailyjoin.application.query.MblNoDupleQuery" resultType="int">
    /* DailyJoinMapper.selectMblNoDupleCnt */
    SELECT COUNT(*)
    FROM (
        SELECT 1
          FROM TB_DAILY_USER
         WHERE CMPNY_CD    = #{cmpnyCd}
           AND MBL_NO_HMAC = #{mblNoHmac}
           AND USE_YN      = 'Y'
         LIMIT 1
    ) T
    </select>

    <!-- 가입 시점 SMS 인증 재검증 - 인증완료(VERIFIED_YN='Y') 레코드의 SMS_ID 조회 -->
    <select id="selectSmsVerifiedSmsId" parameterType="com.prafta.common.cmm.dailyjoin.application.query.SmsConsumeQuery" resultType="string">
    /* DailyJoinMapper.selectSmsVerifiedSmsId */
    SELECT SMS_ID
      FROM TB_SMS_AUTH_CODE
     WHERE MBL_NO_HMAC = #{mblNoHmac}
       AND AUTH_CD     = #{certNo}
       AND VERIFIED_YN = 'Y'
       AND UPDATE_DATE <![CDATA[ >= ]]> DATE_SUB(NOW(), INTERVAL 10 MINUTE)
     ORDER BY UPDATE_DATE DESC
     LIMIT 1
    </select>

    <!-- SMS 인증 레코드 소진 처리 (VERIFIED_YN='Y' -> 'C' 조건부 UPDATE) -->
    <update id="consumeSmsAuth" parameterType="com.prafta.common.cmm.baseinfo.application.command.SmsAuthConsumeCommand">
    /* DailyJoinMapper.consumeSmsAuth */
    UPDATE TB_SMS_AUTH_CODE
    SET VERIFIED_YN = 'C'
        , UPDATE_NO   = 'SYSTEM'
        , UPDATE_DATE = NOW()
    WHERE SMS_ID      = #{smsId}
      AND VERIFIED_YN = 'Y'
    </update>

    <!-- 사업장 계정등록 정책 사용여부 조회 -->
    <select id="selectLinkPolicyUseYn" parameterType="com.prafta.common.cmm.dailyjoin.application.query.LinkPolicyQuery" resultType="string">
    /* DailyJoinMapper.selectLinkPolicyUseYn */
    SELECT USE_YN
    FROM TB_DAILY_USER_LINK_POLICY
    WHERE CMPNY_CD = #{cmpnyCd}
      AND SITE_CD  = #{siteCd}
    </select>

    <!-- 사업장 첫 빈 슬롯 번호 조회 -->
    <select id="selectFirstEmptySlotNo" parameterType="com.prafta.common.cmm.dailyjoin.application.query.EmptySlotQuery" resultType="string">
    /* DailyJoinMapper.selectFirstEmptySlotNo */
    SELECT SLOT_NO
    FROM TB_DAILY_USER_SLOT
    WHERE CMPNY_CD    = #{cmpnyCd}
      AND SITE_CD     = #{siteCd}
      AND USE_YN      = 'Y'
      AND SLOT_STATUS = '01'                    <!-- '01'=비점유(Baim_05 가 생성하는 초기 상태). 과거 '00' 오타 정합 -->
      AND (CURR_USER_CD IS NULL OR CURR_USER_CD = '')
    ORDER BY CAST(SLOT_NO AS UNSIGNED)
    LIMIT 1
    </select>

    <!-- USER_CD 채번 -->
    <select id="selectDailyUserCd" parameterType="com.prafta.common.cmm.dailyjoin.application.query.EmptySlotQuery" resultType="string">
    /* DailyJoinMapper.selectDailyUserCd */
    SELECT CONCAT('D', DATE_FORMAT(NOW(), '%Y%m%d'), FNC_CMM_SEQ_NEXTVAL(#{cmpnyCd}, 'DAILY_USER'))
    FROM DUAL
    </select>

    <!-- 일일사용자 계정 insert -->
    <insert id="insertDailyUser" parameterType="com.prafta.common.cmm.dailyjoin.application.command.InsertDailyUserCommand">
    /* DailyJoinMapper.insertDailyUser */
    INSERT INTO TB_DAILY_USER (
        CMPNY_CD
        , SITE_CD
        , USER_CD
        , USER_ID
        , USER_NM
        , USER_PW

        , MBL_NO_ENC
        , MBL_NO_HMAC
        , MBL_NO_LAST4

        , REG_TYPE
        , USE_YN
        , ACCOUNT_STATUS
        , WORK_EXPIRE_DATE

        , INSERT_NO
        , INSERT_DATE
        , UPDATE_NO
        , UPDATE_DATE
    )
    VALUES (
        #{cmpnyCd}
        , #{siteCd}
        , #{userCd}
        , #{userId}
        , #{userNm}
        , #{userPw}

        , #{mblNoEnc}
        , #{mblNoHmac}
        , #{mblNoLast4}

        , #{regType}
        , #{useYn}
        , #{accountStatus}
        , DATE_FORMAT(NOW(), '%Y%m%d')

        , #{userCd}
        , NOW()
        , #{userCd}
        , NOW()
    )
    </insert>

    <!-- ============================================================
         PRAFTA-app-027-3'(통합형) — TB_USER 통합 INSERT + 중복검증 확장
         ============================================================ -->

    <!-- TB_USER 사용자ID 중복 카운트(활성 USE_YN='Y' 만 검사).
         옵션2: 비활성(만료) 행은 재활성 대상이므로 중복으로 보지 않는다. -->
    <select id="selectTbUserIdDupleCnt" resultType="int">
    /* DailyJoinMapper.selectTbUserIdDupleCnt */
    SELECT COUNT(*)
    FROM (
        SELECT 1
          FROM TB_USER
         WHERE CMPNY_CD = #{cmpnyCd}
           AND USER_ID  = #{userId}
           AND USE_YN   = 'Y'
         LIMIT 1
    ) T
    </select>

    <!-- TB_USER 휴대폰 HMAC 중복 카운트(활성 USE_YN='Y' 만 검사).
         옵션2: 비활성(만료) 행은 재활성 대상이므로 중복으로 보지 않는다. -->
    <select id="selectTbUserMblHmacDupleCnt" resultType="int">
    /* DailyJoinMapper.selectTbUserMblHmacDupleCnt */
    SELECT COUNT(*)
    FROM (
        SELECT 1
          FROM TB_USER
         WHERE CMPNY_CD    = #{cmpnyCd}
           AND MBL_NO_HMAC = #{mblNoHmac}
           AND USE_YN      = 'Y'
         LIMIT 1
    ) T
    </select>

    <!-- 통합형 — TB_USER INSERT.
         EMPLOYMENT_TYPE='DAILY'/AUTH_CD='99999'(5자리 일반근로자)/NODE_CD=NULL/ACCOUNT_STATUS='01'.
         정규 LoginMapper.insertUserInfo 미사용(EMPLOYMENT_TYPE/ACCOUNT_STATUS 미주입) → 전용 INSERT 신설. -->
    <insert id="insertDailyUserToTbUser" parameterType="com.prafta.common.cmm.dailyjoin.application.command.InsertDailyUserCommand">
    /* DailyJoinMapper.insertDailyUserToTbUser */
    INSERT INTO TB_USER (
        CMPNY_CD
        , USER_CD
        , USER_ID
        , USER_NM
        , USER_PW
        , SITE_CD
        , NODE_CD
        , AUTH_CD

        , MBL_NO_ENC
        , MBL_NO_HMAC
        , MBL_NO_LAST4

        , EMPLOYMENT_TYPE
        , USE_YN
        , ACCOUNT_STATUS

        , INSERT_NO
        , INSERT_DATE
        , UPDATE_NO
        , UPDATE_DATE
    )
    VALUES (
        #{cmpnyCd}
        , #{userCd}
        , #{userId}
        , #{userNm}
        , #{userPw}
        , #{siteCd}
        , NULL
        , '99999'

        , #{mblNoEnc}
        , #{mblNoHmac}
        , #{mblNoLast4}

        , 'DAILY'
        , #{useYn}
        , #{accountStatus}

        , #{userCd}
        , NOW()
        , #{userCd}
        , NOW()
    )
    </insert>

    <!-- 통합형 — TB_USER_SITE_AUTH INSERT(해당 사업장 1행). -->
    <insert id="insertTbUserSiteAuth">
    /* DailyJoinMapper.insertTbUserSiteAuth */
    INSERT INTO TB_USER_SITE_AUTH (
        CMPNY_CD
        , USER_CD
        , SITE_CD
        , USE_YN
        , INSERT_NO
        , INSERT_DATE
        , UPDATE_NO
        , UPDATE_DATE
    )
    VALUES (
        #{cmpnyCd}
        , #{userCd}
        , #{siteCd}
        , 'Y'
        , #{userCd}
        , NOW()
        , #{userCd}
        , NOW()
    )
    </insert>

    <!-- 빈 슬롯 점유(CURR_USER_CD 매핑) -->
    <update id="updateDailyUserSlotCurrUserCd" parameterType="com.prafta.common.cmm.dailyjoin.application.command.DailyUserSlotUpdCommand">
    /* DailyJoinMapper.updateDailyUserSlotCurrUserCd */
    UPDATE TB_DAILY_USER_SLOT
    SET CURR_USER_CD = #{userCd}
        , SLOT_TYPE   = #{slotType}
        , SLOT_STATUS = #{slotStatus}
        , UPDATE_NO   = #{userCd}
        , UPDATE_DATE = NOW()
    WHERE CMPNY_CD    = #{cmpnyCd}
      AND SITE_CD     = #{siteCd}
      AND SLOT_NO     = #{slotNo}
      AND USE_YN      = 'Y'
      AND SLOT_STATUS = '01'                    <!-- '01'=비점유에서만 점유('02')로 전이. 과거 '00' 오타 정합 -->
      AND (CURR_USER_CD IS NULL OR CURR_USER_CD = '')
    </update>

    <!-- 필수약관 목록 조회 (TB_TERMS) -->
    <select id="selectRequiredTermsList" resultType="com.prafta.common.cmm.dailyjoin.result.RequiredTermsResult">
    /* DailyJoinMapper.selectRequiredTermsList */
    SELECT
        TERMS_ID        AS termsId
        , TERMS_VERSION AS termsVersion
    FROM TB_TERMS
    WHERE REQUIRED_YN = 'Y'
      AND IFNULL(USE_YN, 'Y') = 'Y'
    </select>

    <!-- 약관 동의 이력 insert -->
    <insert id="insertTermsUserAgrMgmt" parameterType="com.prafta.common.cmm.dailyjoin.application.command.TermsUserAgrCommand">
    /* DailyJoinMapper.insertTermsUserAgrMgmt */
    INSERT INTO TB_TERMS_USER_AGR_MGMT (
        USER_CD
        , TERMS_ID
        , TERMS_VERSION
        , AGR_YN
        , INSERT_NO
        , INSERT_DATE
    )
    VALUES (
        #{userCd}
        , #{termsId}
        , #{termsVersion}
        , #{agrYn}
        , #{userCd}
        , NOW()
    ) AS NEW
    /* PK=(USER_CD,TERMS_ID,TERMS_VERSION). 옵션2 재활성은 USER_CD 를 재사용하므로,
       동일 약관 버전 재동의 시 PK 충돌로 트랜잭션이 롤백되는 것을 방지한다.
       버전이 바뀌면 새 행(이력 보존), 같은 버전 재동의면 동의값/시각만 갱신(멱등). */
    ON DUPLICATE KEY UPDATE
        AGR_YN      = NEW.AGR_YN
        , INSERT_NO   = NEW.INSERT_NO
        , INSERT_DATE = NOW()
    </insert>

    <!-- ============================================================
         옵션2 — 재활성 재가입(만료 일용직이 같은 휴대폰으로 재가입 시
         새 USER_CD 채번 대신 기존 비활성 행을 재활성).
         ============================================================ -->

    <!-- 재활성 대상 USER_CD 조회(비활성 USE_YN='N' 중 최신 1건).
         같은 휴대폰의 비활성 행이 여러 개(레거시)면 가장 최근 가입 1건만 재활성, 나머지는 비활성 유지. -->
    <select id="selectReactivatableDailyUserCd" resultType="string">
    /* DailyJoinMapper.selectReactivatableDailyUserCd */
    SELECT USER_CD
    FROM TB_DAILY_USER
    WHERE CMPNY_CD    = #{cmpnyCd}
      AND MBL_NO_HMAC = #{mblNoHmac}
      AND USE_YN      = 'N'
    ORDER BY INSERT_DATE DESC
    LIMIT 1
    </select>

    <!-- 재활성 — TB_DAILY_USER 기존 행 재사용.
         사업장/ID/이름/비번/휴대폰파생 갱신 + 만료일 오늘로 연장 + 잠금/실패카운트 초기화. -->
    <update id="reactivateDailyUser" parameterType="com.prafta.common.cmm.dailyjoin.application.command.InsertDailyUserCommand">
    /* DailyJoinMapper.reactivateDailyUser */
    UPDATE TB_DAILY_USER
    SET USE_YN                 = 'Y'
        , ACCOUNT_STATUS        = '01'
        , SITE_CD               = #{siteCd}
        , USER_ID               = #{userId}
        , USER_NM               = #{userNm}
        , USER_PW               = #{userPw}
        , MBL_NO_ENC            = #{mblNoEnc}
        , MBL_NO_HMAC           = #{mblNoHmac}
        , MBL_NO_LAST4          = #{mblNoLast4}
        , WORK_EXPIRE_DATE      = DATE_FORMAT(NOW(), '%Y%m%d')
        , WITHDRAWAL_DATE       = NULL
        , PWD_FAIL_CNT          = 0
        , PWD_LOCK_YN           = 'N'
        , PWD_LOCK_EXPIRE_DTIME = NULL
        , UPDATE_NO             = #{userCd}
        , UPDATE_DATE           = NOW()
    WHERE CMPNY_CD = #{cmpnyCd}
      AND USER_CD  = #{userCd}
    </update>

    <!-- 재활성 — TB_USER 기존 통합행 재사용(EMPLOYMENT_TYPE='DAILY' 가드).
         영향행 0이면 = 통합형 이전 레거시 일용직(TB_USER 없음) → 서비스에서 신규 INSERT 폴백. -->
    <update id="reactivateTbUser" parameterType="com.prafta.common.cmm.dailyjoin.application.command.InsertDailyUserCommand">
    /* DailyJoinMapper.reactivateTbUser */
    UPDATE TB_USER
    SET USE_YN          = 'Y'
        , ACCOUNT_STATUS = '01'
        , SITE_CD        = #{siteCd}
        , USER_ID        = #{userId}
        , USER_NM        = #{userNm}
        , USER_PW        = #{userPw}
        , MBL_NO_ENC     = #{mblNoEnc}
        , MBL_NO_HMAC    = #{mblNoHmac}
        , MBL_NO_LAST4   = #{mblNoLast4}
        , UPDATE_NO      = #{userCd}
        , UPDATE_DATE    = NOW()
    WHERE CMPNY_CD        = #{cmpnyCd}
      AND USER_CD         = #{userCd}
      AND EMPLOYMENT_TYPE = 'DAILY'
    </update>

    <!-- 재활성 — TB_USER_SITE_AUTH upsert(사업장 권한 행 활성). 기존 행 있으면 USE_YN 회복. -->
    <insert id="upsertTbUserSiteAuth">
    /* DailyJoinMapper.upsertTbUserSiteAuth */
    INSERT INTO TB_USER_SITE_AUTH (
        CMPNY_CD
        , USER_CD
        , SITE_CD
        , USE_YN
        , INSERT_NO
        , INSERT_DATE
        , UPDATE_NO
        , UPDATE_DATE
    )
    VALUES (
        #{cmpnyCd}
        , #{userCd}
        , #{siteCd}
        , 'Y'
        , #{userCd}
        , NOW()
        , #{userCd}
        , NOW()
    )
    ON DUPLICATE KEY UPDATE
        USE_YN      = 'Y'
        , UPDATE_NO   = #{userCd}
        , UPDATE_DATE = NOW()
    </insert>

</mapper>
