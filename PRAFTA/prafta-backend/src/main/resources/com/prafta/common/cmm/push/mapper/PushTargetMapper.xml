<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.prafta.common.cmm.push.mapper.PushTargetMapper">

    <!--
      PRAFTA-APP-021-3b(W3): TBM 세션의 실제 입실(enter)한 참석자 USER_CD 목록.
      - 입실 완료 = ENTRY_AT IS NOT NULL. DEL_YN='N'. REGULAR/DAILY 무관 전부.
      - CMPNY_CD 스코프. leading 콤마, #{} 바인딩, SELECT * 금지.
    -->
    <select id="selectTbmEnteredUserCds" resultType="java.lang.String">
    /* PushTargetMapper.selectTbmEnteredUserCds */
    SELECT DISTINCT AT.USER_CD
      FROM TB_TBM_ATTENDANCE AT
     WHERE AT.CMPNY_CD   = #{cmpnyCd}
       AND AT.SESSION_CD = #{sessionCd}
       AND AT.DEL_YN     = 'N'
       AND AT.ENTRY_AT IS NOT NULL
       AND NULLIF(TRIM(AT.USER_CD), '') IS NOT NULL
    </select>

</mapper>
