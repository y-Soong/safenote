<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.prafta.common.cmm.siteauth.mapper.SiteAccessMapper">

    <!-- 사업장 접근 권한 확인(TB_USER_SITE_AUTH, USE_YN='Y'). 1 이상이면 접근 가능. -->
    <select id="selectUserSiteAuthCnt" resultType="int">
    /* SiteAccessMapper.selectUserSiteAuthCnt */
    SELECT COUNT(*)
    FROM TB_USER_SITE_AUTH A
    WHERE A.CMPNY_CD = #{cmpnyCd}
      AND A.USER_CD  = #{userCd}
      AND A.SITE_CD  = #{siteCd}
      AND A.USE_YN   = 'Y'
    </select>

    <!--
      접근 가능 사업장 목록(접수함다중사업장권한확장-001).
      TB_USER_SITE_AUTH(USE_YN='Y') ⨝ TB_SITE(USE_YN='Y'). AppAdminAccessMapper.selectAccessibleSites
      와 동일 SQL 형태(app 파일은 무수정).
    -->
    <select id="selectAccessibleSites" resultType="com.prafta.common.cmm.siteauth.result.AccessibleSiteResult">
    /* SiteAccessMapper.selectAccessibleSites */
    SELECT
          S.SITE_CD   AS siteCd
        , S.SITE_NO   AS siteNo
        , S.SITE_NM   AS siteNm
    FROM TB_USER_SITE_AUTH USA
    INNER JOIN TB_SITE S
        ON S.CMPNY_CD = USA.CMPNY_CD
       AND S.SITE_CD  = USA.SITE_CD
    WHERE USA.CMPNY_CD = #{cmpnyCd}
      AND USA.USER_CD  = #{userCd}
      AND USA.USE_YN   = 'Y'
      AND S.USE_YN     = 'Y'
    ORDER BY S.SITE_NM, S.SITE_CD
    LIMIT 100
    </select>

    <!-- 회사 전체 사업장(USE_YN='Y') 목록. master/hr 전사 role fast-path 용(원장 미존재 케이스 방어). -->
    <select id="selectAllCompanySites" resultType="com.prafta.common.cmm.siteauth.result.AccessibleSiteResult">
    /* SiteAccessMapper.selectAllCompanySites */
    SELECT
          S.SITE_CD   AS siteCd
        , S.SITE_NO   AS siteNo
        , S.SITE_NM   AS siteNm
    FROM TB_SITE S
    WHERE S.CMPNY_CD = #{cmpnyCd}
      AND S.USE_YN   = 'Y'
    ORDER BY S.SITE_NM, S.SITE_CD
    LIMIT 100
    </select>
</mapper>
