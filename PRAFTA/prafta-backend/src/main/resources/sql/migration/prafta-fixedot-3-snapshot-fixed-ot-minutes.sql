-- ============================================================================
-- PRAFTA-FIXEDOT-3 — 하도급 근태 공유 스냅샷에 "고정연장 실적(분)" 편입
-- 작성일: 2026-08-12
-- 적용 환경: MySQL 8.0.42 이상 (개발·운영 동시 적용 — feedback_db_migration_apply_both_envs)
-- 참조: .claude/requests/web_requests/작업지시서_근무타입-고정연장근무-도입.plan.md §5-2 (M21 확정)
--
-- 변경 요약
--  1) tb_cmpny_share_snapshot_attd 에 FIXED_OT_MINUTES(분) 추가.
--     정책 ①(실근태가 고정연장 구간을 커버한 분량만 계상)의 파생 산출값을
--     승인 시점에 스냅샷으로 고정 복사한다(원장 아님 — 스냅샷 특성상 시점 고정).
--
-- 공유 스코프 판단(plan §5-2 "동의 스코프 포섭 여부 1회 확인" 이행 결과)
--  - 연동회사 제3자 제공 동의(약관 006) 제공 항목 = "근태 기록: 근무일자, 근무타입,
--    출퇴근 시각, 근태 판정, 초과근무 시간, 연차 사용 내역".
--  - FIXED_OT_MINUTES 는 이미 제공 중인 (근무타입 + 출퇴근 시각)에서 전적으로 파생되는
--    값이고 성격상 "초과근무 시간"에 속하므로 신규 원천 수집·신규 정보 노출이 없다.
--    → 기존 동의 스코프 내로 판단. 다만 약관 문구에 "고정연장" 표현이 없어 문구 보강은
--      운영 판단 사항으로 보고(코드 차원의 게이트 추가는 하지 않음).
--  - "연장 미이행" 배지는 판정 보조 정보이므로 스냅샷에서 제외한다(plan §5-2 확정).
--
-- 멱등성: ALTER 중복 실행 시 에러(Duplicate column). 이미 반영된 환경에서는 건너뛸 것.
-- 적용 순서: BE 재기동 전 선적용 필수(미적용 시 하도급 스냅샷 생성/조회가 신규 컬럼
--            참조로 실패한다).
-- ============================================================================

ALTER TABLE `tb_cmpny_share_snapshot_attd`
      ADD COLUMN `FIXED_OT_MINUTES` int NOT NULL DEFAULT 0
          COMMENT '고정연장 실적(분 — 실근태가 근무타입 고정연장 구간을 커버한 분. 고정연장 미설정 타입은 0)'
          AFTER `OT_MINUTES`;
