-- =============================================================================
-- prafta-platform-3-terms-move.sql
-- 이용약관 관리(Baim_03) 고객 노출 봉쇄 마이그레이션
--
-- 배경
--   - 약관(TB_TERMS)은 회사 스코프가 없는 글로벌 데이터인데, 기존 고객 웹의
--     이용약관 관리 메뉴(MENU_D_ID='Baim_03')로 임의 고객사 master 가 글로벌
--     약관을 편집할 수 있었다.
--   - 백엔드 컨트롤러(/webApi/baim03/*)는 코드에서 제거되었고, 운영자는 신규
--     플랫폼 화면 Platform_02('이용약관 관리', /prafta/platformApi/terms/*)로
--     약관을 관리한다(메뉴 시드는 prafta-platform-2-menu-seed.sql 에 이미 존재).
--   - 본 마이그레이션은 구 Baim_03 메뉴/권한만 봉쇄한다(데이터 TB_TERMS 무변경).
--
-- 주의
--   - DB 직접 적용 금지(작성만). 운영자가 검토 후 수동 적용한다.
--   - '001'/타사 약관 데이터는 절대 변경하지 않는다(본 스크립트는 메뉴/권한만 손댐).
-- =============================================================================

-- -----------------------------------------------------------------------------
-- [적용 전 확인] 영향 범위 사전 점검
-- -----------------------------------------------------------------------------
-- 1) Baim_03 메뉴 권한이 부여된 회사/메뉴 행 수 확인
-- SELECT CMPNY_CD, MENU_D_ID
--   FROM tb_syst_auth_menu
--  WHERE MENU_D_ID = 'Baim_03';

-- 2) 구 메뉴 정의(글로벌)의 현재 사용여부 확인
-- SELECT MENU_D_ID, MENU_VIEW, USE_YN
--   FROM tb_syst_menu_d
--  WHERE MENU_D_ID = 'Baim_03';

-- -----------------------------------------------------------------------------
-- [본 적용]
-- -----------------------------------------------------------------------------
-- (1) 전 회사 대상으로 구 약관관리 메뉴 권한 제거(고객 노출 차단)
DELETE FROM tb_syst_auth_menu
 WHERE MENU_D_ID = 'Baim_03';

-- (2) 구 메뉴 정의 비활성(글로벌 메뉴라 모든 회사 미노출)
UPDATE tb_syst_menu_d
   SET USE_YN = 'N'
 WHERE MENU_D_ID = 'Baim_03';

-- -----------------------------------------------------------------------------
-- [롤백] 봉쇄 해제가 필요할 때(권한 행은 복구 불가하므로 재시드 필요)
-- -----------------------------------------------------------------------------
-- UPDATE tb_syst_menu_d
--    SET USE_YN = 'Y'
--  WHERE MENU_D_ID = 'Baim_03';
--
-- 권한(tb_syst_auth_menu) 복구는 삭제 전 백업이 없으면 수동 재부여가 필요하다.
-- (단, 약관은 글로벌 운영자 전용으로 이전되었으므로 원칙적으로 롤백하지 않는다.)
