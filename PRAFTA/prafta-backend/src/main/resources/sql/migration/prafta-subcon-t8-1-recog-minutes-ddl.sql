-- ============================================================
-- PRAFTA-SUBCON-T8-1: 하도급 공유 스냅샷 — 실근무 인정시간(분) 컬럼 신설
--   실행 주체: 사용자 Workbench 직접 실행 (개발·운영 동시 적용 원칙)
--   컬럼 NULL 허용이므로 코드 배포 순서와 무관하게 안전(코드 배포 전 선적용 권장).
--   구스냅샷은 소급 재계산/백필하지 않는다(스냅샷 불변 원칙) — NULL = 화면 '-' 표시.
-- ============================================================
ALTER TABLE TB_CMPNY_SHARE_SNAPSHOT_ATTD
  ADD COLUMN RECOG_MINUTES int NULL
    COMMENT '정상근무 인정시간(분 — (실제∩스케줄)−휴게−확정 시각연차 겹침, Attd_08 산식 시점고정. NULL=산출불가/구스냅샷)'
    AFTER FIXED_OT_MINUTES;
