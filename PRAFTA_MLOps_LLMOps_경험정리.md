# PRAFTA — MLOps / LLMOps 경험 정리

> 최종 갱신: 2026-08-19 · 용어가 낯설면 `PRAFTA_AI_용어설명.md` 를 먼저 참고
>
> **스코프 선언(정직하게)**: 모델을 **학습·파인튜닝하지 않았다.** 한 일은 ⑴ 오픈소스 임베딩 모델을
> 셀프호스팅 서빙하고 그 모델이 먹을 데이터 파이프라인을 구축·운영한 것(MLOps 범주), ⑵ 상용 LLM을
> RAG 구조로 프로덕션에 통합하고 비용·품질·안정성을 운영한 것(LLMOps 범주)이다.
> **경험의 무게중심은 LLMOps 쪽**이며, 이력서에서도 그렇게 배치하는 것이 정확하다.

---

## 0. 시스템 한눈에

산업안전 SaaS(PRAFTA)에 **"공공 안전자료를 근거로 위험성평가·TBM 교육안을 도출하는 AI"** 를 붙였다.

```
[적재 · 오프라인 배치]                      [질의 · 실시간]
공공데이터(data.go.kr, KOSHA)                 사용자 질의
  → 라이선스 게이트(사람 확정)                   → 임베딩(BGE-m3)
  → 전처리·PII마스킹·청킹                        → 벡터 검색(pgvector)
  → 임베딩(BGE-m3 / TEI 셀프호스팅)              → 유사도 0.40 필터
  → pgvector 적재(멱등 UPSERT)                   → 프롬프트 조립(근거+출처+신뢰등급)
  → HNSW 코사인 인덱스                           → 생성(HCX-005)
        └──────── 검색 대상 제공 ───────────────┘  → 각주 검증·감사기록·쿼터 차감
```

| 구성요소 | 선택 | 서빙 주체 |
|---|---|---|
| 임베딩 모델 | BGE-m3 (1024차원, 오픈소스) | **TEI로 셀프호스팅** (무료) |
| 벡터 저장·검색 | PostgreSQL + pgvector, HNSW | Docker 컨테이너 |
| 생성 LLM | HyperCLOVA X (HCX-005) | 네이버 CLOVA Studio (관리형 API, 유료) |
| 서비스 백엔드 | Spring Boot + MyBatis | 네이티브 (MySQL과 별개로 2차 데이터소스 연결) |

**설계 판단**: 호출량이 압도적으로 많은 임베딩(코퍼스 수천 건 적재 + 매 질의)은 **셀프호스팅해 비용 0**,
크고 비싼 생성 모델은 **관리형 API를 구매**. 이 분리가 비용 구조의 핵심.

---

## 1. MLOps 범주 — 모델 서빙 + 데이터 파이프라인

### 1-1. 임베딩 모델 셀프호스팅 서빙

- HuggingFace **TEI**(Text Embeddings Inference)로 **BGE-m3(1024차원)** 임베딩 서버를 Docker로 직접 서빙
- 왜 셀프호스팅인가 — 선택지 비교 후 결정
  1. 외부 임베딩 API: 호출당 과금 + **안전 코퍼스·질의가 외부로 유출**
  2. Java 앱에 모델 내장: 임베딩 생태계가 Python 중심이라 번거롭고 앱 메모리 잠식
  3. **전용 추론 서버 분리** ← 채택. 백엔드는 REST 한 번으로 임베딩을 얻고, **ML 라이브러리 의존이 0**
- 차원 검증 게이트를 **입구·출구 양쪽**에 배치: 적재 스크립트와 백엔드 클라이언트 모두 1024차원이
  아니면 즉시 실패시킨다. 잘못된 차원이 섞이면 **검색이 에러 없이 조용히 망가지므로** fail-fast.

### 1-2. 벡터 DB 구축·운영

- **PostgreSQL 16 + pgvector 확장**, HNSW 코사인 인덱스. 서비스 DB(MySQL)와 분리된 2차 데이터소스
- **전문 벡터 DB(Pinecone·Milvus 등) 대신 pgvector를 고른 근거**
  1. 코퍼스 규모(수천 건)에 전문 벡터 DB의 강점(수억 벡터)이 불필요
  2. 익숙한 RDB 운영 지식·SQL·백업 체계 재사용 → 새 인프라 운영 부담 회피
  3. **★벡터 유사도 정렬 + 메타 필터 + 출처 조인을 한 SQL로 처리** — 실제 검색 쿼리에
     `ORDER BY embedding <=>` 옆에 `WHERE use_yn/domain_tag/신뢰등급` 필터와 출처 테이블
     `LEFT JOIN`이 함께 들어간다. 전문 벡터 DB였다면 별도 구현이 필요했을 부분
- 벡터 검색 SQL의 **모든 동적 조건을 파라미터 바인딩**으로 처리(문자열 연결 금지 = 주입 방지)

### 1-3. 코퍼스 수집·적재 파이프라인

**단계**: `수집(raw) → 레지스트리 게이트 → 전처리·마스킹·청킹(processed) → 임베딩 → 적재 → 인덱스`

- **다형식 수집**: data.go.kr API(JSON), CSV(cp949), XLSX(시트별 분기), **PDF**
  - PDF는 pdfplumber로 표 괘선을 읽어 셀을 복원하고 **병합셀 forward-fill** → 332행 전량 청크화
  - 소스 ID를 데이터셋 ID로 고정해 **자료 버전 갱신을 추적**(SIF 4,432건 → 6,032건 증분 이력)
- **라이선스 거버넌스 게이트** — "판정은 사람, 실행은 코드"
  - 사람이 레지스트리에 `license_checked=TRUE` 로 확정한 자료만 파이프라인이 처리
  - **게이트가 라이선스를 해석하지 않는다.** 사람의 판정 결과를 강제하는 검문소 역할이며,
    필수필드·원본존재·track↔라이선스 정합성도 함께 검사하고 **데이터는 변경하지 않는다**
- **PII 마스킹** — 삭제가 아닌 **익명 치환**(문장 구조 보존 → 임베딩 맥락 유지)
  - 업체명 `○○건설`, 성명 `○○○`, 주소는 **시군구까지 보존**(지역 위험패턴 검색은 살림)
  - 정규식 경계 처리: `X건설` 뒤에 한글이 없을 때만 매칭 → **"대우건설"(회사)은 잡고
    "건설현장·산업안전"(일반명사)은 건드리지 않음**
  - 애매하면 치환 대신 **필드째 제거 후 로그** (미마스킹 데이터를 산출물에 남기지 않는 하드가드)
- **필드 용도 분리**: 검색 대상(CONTENT) / 필터(domain_tag) / **감사·표시용(meta_json, LLM 미투입)** /
  출처 역추적(source_locator). LLM 프롬프트에는 CONTENT만 투입
- **유해요인을 "도출하지 않기로" 한 결정** — 사고 경위 자유텍스트에서 규칙·LLM으로 유해요인을
  추출할 수도 있었으나, **오분류·환각 위험 때문에 전용 컬럼이 있는 자료만 매핑**하고 없으면 NULL.
  검색은 CONTENT 임베딩으로 돌기 때문에 품질 영향 없음. **"AI를 안 쓰기로 한 결정"**
- **멱등 적재**: `chunk_id = {소스ID}_{sha256(소스ID|locator|CONTENT)[:16]}` 결정적 ID +
  `ON CONFLICT DO UPDATE`. 순번 ID였다면 재실행마다 중복이 쌓였을 구조를 원천 차단
- **E2E 스모크 테스트**: 임베딩 → 적재 → 코사인 검색 관통을 한 스크립트로 자동 검증(재실행 안전)

### 1-4. 배치 안정성 — "40분짜리 배치는 반드시 죽는다"

6천 청크 임베딩에 수십 분(CPU 추론, 건당 수백 ms). 중단을 전제로 3중 설계:

| 장치 | 역할 | 없으면 |
|---|---|---|
| 200행마다 커밋 | 중간 저장 | 중단 시 전체 소실 |
| **resume** | 기적재 chunk_id는 **임베딩조차 건너뜀** | 재실행 시 소요 2배 |
| UPSERT + 결정적 ID | 겹쳐도 덮어쓰기 | 재실행마다 중복 누적 |

→ 셋이 맞물려 **"실패하면 그냥 다시 돌리면 된다"** 가 성립. resume의 핵심은 "넣지 않는다"가 아니라
**"비싼 계산을 하지 않는다"** 는 점(병목은 DB가 아니라 임베딩).

**배치 크기를 두 층으로 분리**: 클라이언트 요청 단위 16문장(통신 오버헤드 ↔ 메모리·재시도 손실 균형),
DB UPSERT 단위 200행(왕복 효율 ↔ 중단 시 손실 균형). 병목이 다른 곳에 있으므로 같은 값을 쓰지 않았다.

**방어**: TEI 호출 3회 재시도 + 점증 백오프, 타임아웃 120초, VARCHAR 한도 초과분 절단.

### 1-5. 인프라 구성 판단

- **Docker는 AI 스택에만 적용** (컨테이너 2개: TEI, pgvector)
  - 기준: **"이종 스택이냐"** — TEI(Rust 바이너리)·pgvector(Postgres 확장 컴파일)는 네이티브 설치가
    번거로워 격리. 이미 잘 돌던 Spring·Vue·MySQL은 옮기지 않음. **전면 도입 대신 필요한 곳에만**
  - 볼륨 2개의 역할 분리(DB 데이터 영속 / 2GB 모델 캐시), 기존 서비스와 포트 충돌 회피(5432·8090)
  - 로컬 자격증명은 throwaway임을 명시하고 **운영은 전부 환경변수 주입**으로 분리
- **AI 스택을 코어 서비스와 별도 EC2로 분리**, 상시 가동이 불필요하므로 **stop-start로 비용 통제**

---

## 2. LLMOps 범주 — RAG 기반 LLM 서비스 운영

### 2-1. RAG 채택 근거

| 방법 | 문제 |
|---|---|
| LLM에 그냥 질의 | 도메인 자료를 모름 + 환각 + **출처 제시 불가** |
| 파인튜닝 | 비용 + 자료 갱신마다 재학습 + **지식이 모델에 녹아 근거 추적 불가** |
| **RAG** ← 채택 | 모델 불변, 질의마다 근거 검색·주입. **자료 교체가 DB 교체로 끝나고 근거 추적 가능** |

안전 도메인은 "어느 자료에 근거했는가"를 답하지 못하면 쓸 수 없다 — 이것이 결정적 이유.

### 2-2. RAG 파이프라인 구현

`질의 임베딩 → pgvector top-K → 트랙/신뢰등급 필터 → 유사도 임계 필터 → 프롬프트 조립 → 생성 → 각주 검증`

**핵심은 가운데 "증강(Augmented)" 단계** — 검색 결과를 어떤 모양으로 LLM에 보여줄지가 RAG의 실체다.
조립되는 프롬프트에 **근거마다 번호·출처명·신뢰등급을 함께 실어**, LLM이 자료의 무게를 다르게
다루도록 지시한다(예: "자율신고형은 단정하지 말고 약하게 인용").

### 2-3. 환각 억제 4중 장치

**① 그라운딩 임계값(cosine 0.40)**
벡터 검색은 관련 자료가 없어도 **항상 "가장 가까운 것"을 돌려준다.** 그 억지 근거가 오히려 오답의
재료가 되므로, 임계 미만은 근거 채택 자체를 거부. 고정값이 아니라 **환경변수 튜닝 knob**.

**② 각주를 LLM에게 맡기지 않음 (가장 중요)**
LLM이 "근거는 [3]번"이라 답해도 **[3]번이 실재하지 않을 수 있다(번호 환각).**
→ 서버가 LLM이 단 마커를 실제 근거 목록과 **대조**해, 실재하는 것만 출처로 조립하고 **없는 마커는 제거**.
**최종 화면의 출처는 LLM 생성물이 아니라 서버 검증 결과물.** LLM은 주장만 하고, 판정은 코드가 쥔다.

**③ ABSTAIN — 포기할 출구를 형식으로 강제**
"지어내지 마"라고만 하면 LLM은 대체로 뭔가를 지어낸다(포기할 방법이 없으므로).
`ABSTAIN: Y` 를 출력 형식에 명시해 **"모르면 모른다고 할 수 있게"** 만들고, 그 경우 근거부재
표시(`abstained`)와 함께 응답·기록한다.

**④ 3단계 폴백**
`근거기반 생성 → (형식 붕괴 시) 동일 프롬프트 1회 재시도 → (또 실패/무관 판정 시) 자유생성`
빈 화면을 주지 않되 **"근거 없는 답"임을 반드시 표시**. 재시도·폴백으로 발생한 **LLM 호출 건마다
감사 로그를 남겨 기록 누락이 없다.**

### 2-4. 출처·라이선스 거버넌스

- 답변에 **citation**(기관명·원문링크·라이선스) 부착, 신뢰등급(규범형/집계형/자율신고형) 표기
- **verbatim(변경금지) 자료의 구조적 격리**: 원문을 생성 호출에 **아예 투입하지 않는다.**
  대신 "후보 중 번호만 고르는 전용 호출"에만 노출하고 서버가 **숫자 외 출력을 전부 폐기**,
  화면에는 서버가 DB 원문을 그대로 렌더 → **LLM 출력물에 원문이 한 글자도 포함될 수 없어
  원문 변형 배포가 구조적으로 불가능**. 정책을 프롬프트 부탁이 아니라 구조로 보장한 사례

### 2-5. 프롬프트 운영

- 용도별 시스템 프롬프트 4종 분리: 근거기반 생성 / 자유생성 / verbatim 선택 / 이미지 확인
- 버전 반복: 위험성평가 v3.x → **v3.11**, TBM **v4**. 실패 관찰 → 지시문 개정의 누적
- **출력 형식 설계**: JSON을 폐기하고 줄 단위 프로토콜 채택 (→ §3-③ 트러블슈팅)
- AI 결과의 **확정 게이트**: 저장 시에만 확정(`SAVED_YN`), 미확정 항목은 후속 AI 처리에서 스킵

### 2-6. 멀티모달(VLM) 경로

VLM은 별도 모델이 아니라 **HCX-005가 가진 능력**이며, 같은 클라이언트에 이미지 첨부 여부만 다르다.

| 경로 | 근거 조달 | 환각 억제 |
|---|---|---|
| 위험성평가 | RAG(벡터 검색) | 유사도 임계 + 각주 검증 |
| TBM 교육자료 분석 | **첨부 이미지 자체** | "첨부에서 실제로 보이는 내용만" 프롬프트 |

**제약 관리(실무 포인트)**: 요청당 이미지 ≤5 클램프 / 긴 변 2240px 한도 대비 보수적 2000px 다운스케일 /
**디코딩 폭탄 방어**(헤더로 픽셀 수 선검사, 60MP 초과 시 전체 디코드 없이 거부) /
PDF **스트라이드 샘플링**(5페이지 간격, 최대 10페이지)으로 토큰 통제

### 2-7. 비용 거버넌스와 관측성

- **테넌트별 월 토큰 쿼터**(월 80만, 특정사 무제한) — 멀티테넌트 SaaS에서 AI 비용 폭주 차단
- **호출 단위 감사 로그**(`tb_ai_call`): 회사·기능·입출력/캐시 토큰·단가 기반 비용·사용 근거 청크 ID·
  abstain 여부. **비용 정산과 품질 분석의 원천 데이터**
- **PII-aware 관측성**: 질의 원문을 저장하지 않고 **길이만** 기록. 무염 해시조차 역추적 리스크로 배제 —
  관측성과 프라이버시의 트레이드오프를 설계 단계에서 결정
- **튜닝 근거 로그**: 호출마다 `최상위 유사도 점수 / 임계값 / 채택 건수`를 남겨,
  임계값 조정을 감이 아니라 **점수 분포 데이터로** 판단할 수 있게 함

### 2-8. 서빙 안정성

- **기능 게이트**(`prafta.ai.llm.enabled`, 기본 OFF): OFF면 LLM 클라이언트 **빈 자체가 생성되지 않고**
  엔드포인트가 명시적 에러계약으로 응답. API 키 없이도 서버가 정상 기동하며, on/off가 **배포 없이 환경변수**로
- 샘플링 파라미터(temperature **0.2** — 근거 기반 답변의 결정성 확보 / top-p / repetition penalty),
  모델 tier, 호스트, 단가까지 전부 환경변수 오버라이드
- **비동기 잡 + 부분 실패 격리**: TBM 다건 분석은 저장 즉시 응답하고 백그라운드 처리(`@Async`).
  항목마다 즉시 커밋해 **한 항목 실패가 앞선 성공을 롤백하지 않게** 트랜잭션 경계를 의도적으로 분리

---

## 3. 트러블슈팅 사례 (면접 핵심 재료)

### ① TEI 무한 재시작 루프 — 컨테이너는 살아 있는데 전부 실패

- **증상**: 임베딩 서버 기동 후 모든 요청 실패. `docker ps` 상 컨테이너는 정상으로 보임
- **원인**: `--max-batch-tokens` 기본값(16384)이 CPU·WSL2(가용 ~4.8GB) 환경에 과도.
  TEI가 **워밍업 시 최대 배치 크기만큼 메모리를 선점**하려다 OOM → 재시작 정책이 이를 무한 반복.
  **재시작 직후 상태라 "떠 있는 것처럼" 보이는 것이 진단을 어렵게 함**
- **해결**: 실제 코퍼스 청크 크기(≤1200토큰)를 근거로 **4096으로 하향**. 한 배치에 청크 3개가 들어가
  처리량 손실 없이 안정화. 초과 입력은 `--auto-truncate` 로 방어
- **교훈**: 값을 임의로 낮춘 게 아니라 **실 데이터 크기에서 안전 하한을 역산**했다는 점. 그리고
  `docker ps`가 아니라 **재시작 로그**를 봐야 하는 유형의 장애가 있다는 것

### ② HNSW 사후필터 기아 — 에러도 로그도 없는 "조용한 0건"

- **증상**: DB에 분명히 존재하는 소수 트랙(verbatim 등) 자료가 검색에서 **0건**으로 나옴.
  예외도 경고도 없음
- **원인**: 근사 검색(HNSW)이 **후보를 먼저 좁히고(기본 ef_search 40) 나중에 필터를 적용**하는 순서.
  후보 40개 안에 해당 트랙이 없으면 그대로 0건이 된다. **소수 자료일수록 구조적으로 탈락**
- **해결**: pgvector의 **iterative scan**(`hnsw.iterative_scan = relaxed_order`) — 필터 통과분이
  LIMIT을 채울 때까지 탐색을 계속하도록 변경. 라이브 DB 적용 + `init.sql` 에도 기록해
  **볼륨 재생성 시 설정 유실 방지**
- **교훈**: **0건은 "정상 응답"과 구분되지 않는다.** 근사 검색 + 사후 필터 조합의 구조적 함정을
  이해해야 발견 가능한 유형

### ③ HCX JSON 파싱 상습 실패 → 출력 프로토콜 재설계

- **증상**: LLM에 JSON 응답을 요구했으나 배열 미폐합 등 **괄호 불일치가 확률적으로 반복** → 파싱 실패
- **해결**: JSON을 폐기하고 **줄 단위 프로토콜**로 전환 (`H:` 유해요인 / `M:` 개선안 / `[n]` 각주).
  괄호가 없으므로 구조가 깨질 수 없고, **한 줄이 손상돼도 그 줄만 버리면 됨**
- **교훈**: **LLM에 복잡한 구조를 요구하지 말고, 틀려도 복구 가능한 단순 형식을 설계한다.**
  더불어 파싱 실패 시 1회 재시도 → 자유생성 폴백을 걸어 사용자에게 빈 화면이 가지 않게 함

### ④ 조건부 빈 2개 공존 → LLM 호출 전멸 (운영 장애)

- **증상**: 기동·헬스체크는 정상인데 **LLM 호출이 런타임에 전멸.** 로컬(게이트 OFF)에서 미재현
- **원인**: 서로 다른 기능의 REST 클라이언트 빈 2개(SMS·LLM)가 공존하면서 지연 조회 충돌
- **해결**: `@Qualifier` 명시 주입으로 핫픽스 (커밋 `bb82cfbd`)
- **교훈**: **조건부 빈을 추가할 때는 모든 기능 게이트를 ON으로 켠 형상에서 기동 검증**해야 한다.
  게이트 OFF 상태의 로컬 테스트는 이 유형을 절대 재현하지 못한다

---

## 4. 기술 선택과 근거 (의사결정 요약)

| 결정 | 대안 | 채택 근거 |
|---|---|---|
| RAG | 파인튜닝 | 자료가 계속 갱신되는 도메인 + **근거 추적 가능성**(안전 도메인 필수) |
| 임베딩 셀프호스팅 | 외부 임베딩 API | 호출량이 많아 비용 유리 + **자료 외부 유출 회피** |
| 생성은 관리형 API | LLM 셀프호스팅 | 대형 모델 직접 서빙은 비용·운영 부담 과다 |
| pgvector | 전문 벡터 DB | 규모 대비 과함 + **벡터+메타필터+조인을 한 SQL로** |
| Docker는 AI 스택만 | 전면 컨테이너화 | 이종 스택만 격리, 잘 돌던 것은 건드리지 않음 |
| 유해요인 자동도출 안 함 | 규칙·LLM 추출 | **오분류·환각 위험** > 얻는 이득 |
| 줄 단위 출력 프로토콜 | JSON | 모델의 실제 실패 양상에 형식을 맞춤 |

---

## 5. 이력서 문구 (요약)

- **MLOps** — 오픈소스 임베딩 모델(BGE-m3)을 TEI로 셀프호스팅하고, 라이선스 거버넌스 게이트와
  PII 마스킹을 갖춘 공공데이터 수집·전처리·임베딩·pgvector 적재 파이프라인을 구축. 결정적 ID 기반
  멱등 UPSERT와 resume 설계로 수십 분 배치의 중단 복구를 보장.
- **LLMOps** — HyperCLOVA X 기반 RAG 서비스를 설계·운영. 유사도 임계값·ABSTAIN·3단계 폴백·
  **서버 측 각주 검증**으로 환각을 억제하고, 테넌트별 토큰 쿼터와 호출 단위 비용 추적, PII 비저장
  관측성, 멀티모달 입력 제약 처리, 비동기 잡 부분 실패 격리를 구현.
- **트러블슈팅** — TEI OOM 재시작 루프(배치 토큰 상한 역산), HNSW 사후필터 기아(iterative scan),
  LLM JSON 파싱 실패(출력 프로토콜 재설계), 조건부 빈 충돌로 인한 런타임 호출 전멸(`@Qualifier`).

---

## 6. 예상 질문 대비

**Q. 왜 파인튜닝을 안 했나?**
→ 코퍼스가 계속 갱신되는 도메인이라 재학습 비용이 반복 발생하고, 무엇보다 **파인튜닝은 지식이 모델
가중치에 녹아 근거를 추적할 수 없다.** 안전 도메인에서 "어느 자료에 근거했는가"를 답하지 못하면
서비스로 쓸 수 없어 RAG를 택했다.

**Q. 환각은 어떻게 막았나?**
→ 4중이다. ⑴ 유사도 임계 미만 근거는 채택 거부 ⑵ **LLM이 단 각주를 서버가 실재 여부로 대조·검증**
⑶ ABSTAIN 출구를 출력 형식으로 강제 ⑷ 실패 시 3단계 폴백 + 근거부재 표시.
핵심은 **LLM에게 판단을 맡기지 않고 검증을 코드가 쥔 것**.

**Q. 임계값 0.40은 어떻게 정했나?**
→ 고정 진리값이 아니라 환경변수 튜닝값이다. 호출마다 **최상위 유사도 점수와 채택 건수를 로그로
남겨** 분포를 보고 조정하도록 설계했다. 높으면 정상 질의가 근거부재로 빠지고, 낮으면 필터가 무의미해진다.

**Q. 비용은 어떻게 통제했나?**
→ 구조적으로는 임베딩을 셀프호스팅해 대량 호출 구간의 비용을 0으로 만들었고, 운영상으로는
테넌트별 월 토큰 쿼터 + 호출 단위 비용 로그, 입력 측에서는 top-K 상한·PDF 페이지 샘플링·
이미지 다운스케일·출력 토큰 클램프로 토큰량 자체를 통제했다.

**Q. 규모가 커지면 무엇을 바꾸겠나?**
→ ⑴ HNSW 파라미터(m, ef_search) 튜닝 — 현재는 기본값 ⑵ 임베딩을 GPU 인스턴스로 이전
(현재 CPU라 6천 건에 수십 분) ⑶ 코퍼스가 수십만 건을 넘으면 전문 벡터 DB 재검토
⑷ 이미지 태그 고정 등 재현성 강화. 지금 규모에서는 과잉이라 의도적으로 하지 않았다.
